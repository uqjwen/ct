# `xx_lsu_ld_dc` 功能点与 Test Plan

范围为 DC 流水寄存、D-cache tag 判定、borrow、LQ 创建、restart、forward 元数据和 DA 接口。scoreboard 以 `{IID, generation, inst/borrow, addr, hit way}` 追踪。

| 二级功能点 | 三级功能点 | 功能点描述 | 测试方法和配置说明 | 优先级 |
|---|---|---|---|---|
| 流水级控制 | EX1→EX2 锁存 | AG 请求在 DC 保存 payload，full/check flush 可杀年轻指令；比较与清除见 `srcs/xx_lsu_ld_dc.sv:1109`～`1131` | 连续互异 IID 请求交叉 stall、full/check flush 和 borrow；检查 killed owner 不进入 DA | P0 |
| Borrow | owner 与 gate | VB/SNQ/ICC/WMB/MMU/PRQ 等可借用读口，valid 与 gate/payload owner 必须一致；状态见 `srcs/xx_lsu_ld_dc.sv:1157` | 每个来源单独和合法竞争，另负向注入 valid-only；断言 valid implies gate 且仅一个 payload owner | P0 |
| D-cache tag | 四 way 命中 | tag 比较形成四 way `ldc_hit_way` 和 settle way；选择见 `srcs/xx_lsu_ld_dc.sv:1250` | 四 way 单 hit、miss、多 hit、X tag，交叉 cache disable/NC；正式场景断言 onehot0 | P0 |
| Unit-stride | way 保存 | unit-stride 与 borrow 都能决定后级 settle way，必须与 AG 保存的 tag 结果一致；见 `srcs/xx_lsu_ld_dc.sv:1250` | unit-stride 四 way 花纹后紧随 scalar，检查 way 不粘连、512-bit 数据只使用目标 way | P0 |
| Byte mask | 四区传递 | 向 DA 传递四组 bytes/reg-bytes valid，标量只消费第一区；输出声明与逻辑见 `srcs/xx_lsu_ld_dc.sv:1279` | US/non-US/scalar 连续切换，四区使用互异 mask；检查适用字段逐 bit 保真和非适用字段不被消费 | P1 |
| LQ 创建 | 指针与资格 | DC 对 load 形成 LQ create DP/gate/function valid，并传递 one-hot entry；创建逻辑见 `srcs/xx_lsu_ld_dc.sv:1559` | LQ empty/one-left/full，create 与 flush/restart 同拍；检查 accept 对应唯一 entry，失败不进入 DA | P0 |
| Restart | LQ full/TLB busy | LQ full、依赖失败、uTLB busy 等阻止 DA valid并产生重启；TLB gate 输出见 `srcs/xx_lsu_ld_dc.sv:581` | 每种原因单独与两两交叉，检查原因优先级、一次 wake/restart、无 completion | P0 |
| 异常传播 | AF mask/extra | 将 AG 异常与 DC 新 MMU access fault 组合，并在适用条件下 mask；见 `srcs/xx_lsu_ld_dc.sv:1469`～`1472` | misalign/PF/AF/NC/borrow 交叉，检查异常不丢失、不重复且 mtval owner 正确 | P0 |
| Forward 元数据 | SQ/WMB 选择 | 保存 SQ/WMB forward valid 和 byte mask供 DA 合并；接口见 `srcs/xx_lsu_ld_dc.sv:532`～`534` | SQ-only、WMB-only、无 forward 和冲突负向场景，使用互异数据花纹检查 source/payload 一致 | P1 |
| DA 接口 | inst/borrow 分流 | DC 向 DA 发送指令或 borrow 类型、hit way、数据请求和异常元数据；接口集中于 `srcs/xx_lsu_ld_dc.sv:191`～`286` | back-to-back inst/borrow，DA 随机反压；stage scoreboard 检查每次 accept 的完整 payload hash | P0 |
| Debug | 地址采集脉冲 | debug load 可建立地址 trace/circular buffer 请求；创建输出见 `srcs/xx_lsu_ld_dc.sv:304`～`307` | debug/normal/debug 连续三拍，检查 valid 为单次脉冲、地址不重复且 flush 后无迟到 | P1 |
| Clock/Reset | gate 覆盖 | inst、borrow、LQ create、TLB busy 和 debug payload 更新都必须有对应 gateclk；相关输出见 `srcs/xx_lsu_ld_dc.sv:176`～`181` | ICG on/off/scan 与 reset 释放边界随机激励，检查功能 valid 高时状态寄存器实际采样 | P1 |

P0 用例必须在集成 `xx_lsu_dcache_arb`、LQ 和 DA 的环境中运行；单模块随机激励不足以证明 borrow owner 合同。

<!-- DC-INTERACTION-2.1-BEGIN -->

## Interaction 2.1 叶级场景与执行合同

共72行；每个父功能点6行。状态只允许 `BLOCKED_NO_VCS` 或 `PENDING_FULL_CHIP`。

|场景|setup/周期|当|则|checker/coverage/关闭|
|---|---|---|---|---|
|`DC-FP-01-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lag_ldc_ex1_inst_vld=1` 且无flush时|则 C1 `ldc_lda_ex2_inst_vld=1` 且 `ldc_ex2_iid=lag0_ex1_iid`|`CHK_DC_FP01_OWNER` / `COV_DC_FP01_OWNER`；每个accept仅产生一个同IID的EX2 owner|
|`DC-FP-01-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_inst_vld` 保持二态且只归属于同一owner|`CHK_DC_FP01_OWNER` / `COV_DC_FP01_OWNER`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-01-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_inst_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP01_OWNER` / `COV_DC_FP01_OWNER`；winner唯一，loser payload零次误消费|
|`DC-FP-01-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_inst_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP01_OWNER` / `COV_DC_FP01_OWNER`；flush后的completion/create/forward计数均不增加|
|`DC-FP-01-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_inst_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_inst_vld)=0`|`CHK_DC_FP01_OWNER` / `COV_DC_FP01_OWNER`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-01-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_inst_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP01_OWNER` / `COV_DC_FP01_OWNER`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-02-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `dcache_arb_ldc_borrow_vld=1` 且 `dcache_arb_ldc_borrow_vld_gate=1` 时|则 C1 `ldc_ex2_borrow_vld=1` 且borrow payload来自同一请求|`CHK_DC_FP02_BORROW_GATE` / `COV_DC_FP02_BORROW`；valid-only负向注入必须触发gate合同检查|
|`DC-FP-02-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `dcache_arb_ldc_borrow_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_ex2_borrow_vld` 保持二态且只归属于同一owner|`CHK_DC_FP02_BORROW_GATE` / `COV_DC_FP02_BORROW`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-02-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `dcache_arb_ldc_borrow_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_ex2_borrow_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP02_BORROW_GATE` / `COV_DC_FP02_BORROW`；winner唯一，loser payload零次误消费|
|`DC-FP-02-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `dcache_arb_ldc_borrow_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_ex2_borrow_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP02_BORROW_GATE` / `COV_DC_FP02_BORROW`；flush后的completion/create/forward计数均不增加|
|`DC-FP-02-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `dcache_arb_ldc_borrow_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_ex2_borrow_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_ex2_borrow_vld)=0`|`CHK_DC_FP02_BORROW_GATE` / `COV_DC_FP02_BORROW`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-02-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `dcache_arb_ldc_borrow_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_ex2_borrow_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP02_BORROW_GATE` / `COV_DC_FP02_BORROW`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-03-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lag_ldc_ex1_inst_vld=1` 且 `cp0_lsu_dcache_en=1` 时|则 C1 `ldc_hit_way` 必须满足onehot0且 `ldc_lda_ex2_dcache_hit` 等于其归约或|`CHK_DC_FP03_HIT_ONEHOT` / `COV_DC_FP03_HIT_WAY`；0/1路命中合法，2至4路命中必须被检测|
|`DC-FP-03-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_hit_way` 保持二态且只归属于同一owner|`CHK_DC_FP03_HIT_ONEHOT` / `COV_DC_FP03_HIT_WAY`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-03-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_hit_way` 只反映文档定义的winner且不含X|`CHK_DC_FP03_HIT_ONEHOT` / `COV_DC_FP03_HIT_WAY`；winner唯一，loser payload零次误消费|
|`DC-FP-03-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_hit_way` 不得产生被flush owner的新副作用|`CHK_DC_FP03_HIT_ONEHOT` / `COV_DC_FP03_HIT_WAY`；flush后的completion/create/forward计数均不增加|
|`DC-FP-03-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_hit_way` 仅在所属accept窗口有效且 `$isunknown(ldc_hit_way)=0`|`CHK_DC_FP03_HIT_ONEHOT` / `COV_DC_FP03_HIT_WAY`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-03-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_hit_way` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP03_HIT_ONEHOT` / `COV_DC_FP03_HIT_WAY`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-04-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lag_ldc_ex1_inst_us=1` 且 `lag_ldc_ex1_us_way` 为one-hot时|则 C1 `ldc_lda_ex2_settle_way` 与输入way编码一致且 `ldc_lda_ex2_inst_us=1`|`CHK_DC_FP04_US_WAY` / `COV_DC_FP04_US_WAY`；四way后接scalar不得粘连旧way|
|`DC-FP-04-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_settle_way` 保持二态且只归属于同一owner|`CHK_DC_FP04_US_WAY` / `COV_DC_FP04_US_WAY`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-04-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_settle_way` 只反映文档定义的winner且不含X|`CHK_DC_FP04_US_WAY` / `COV_DC_FP04_US_WAY`；winner唯一，loser payload零次误消费|
|`DC-FP-04-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_settle_way` 不得产生被flush owner的新副作用|`CHK_DC_FP04_US_WAY` / `COV_DC_FP04_US_WAY`；flush后的completion/create/forward计数均不增加|
|`DC-FP-04-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_settle_way` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_settle_way)=0`|`CHK_DC_FP04_US_WAY` / `COV_DC_FP04_US_WAY`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-04-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_settle_way` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP04_US_WAY` / `COV_DC_FP04_US_WAY`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-05-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lag_ldc_ex1_inst_vld=1` 且四区mask使用互异花纹时|则 C1 `ldc_lda_ex2_bytes_vld` 至bytes_vld3逐区保持原花纹|`CHK_DC_FP05_MASK_PASS` / `COV_DC_FP05_MASK`；适用mask逐bit保真，非US不得消费高区旧值|
|`DC-FP-05-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_bytes_vld` 保持二态且只归属于同一owner|`CHK_DC_FP05_MASK_PASS` / `COV_DC_FP05_MASK`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-05-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_bytes_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP05_MASK_PASS` / `COV_DC_FP05_MASK`；winner唯一，loser payload零次误消费|
|`DC-FP-05-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_bytes_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP05_MASK_PASS` / `COV_DC_FP05_MASK`；flush后的completion/create/forward计数均不增加|
|`DC-FP-05-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_bytes_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_bytes_vld)=0`|`CHK_DC_FP05_MASK_PASS` / `COV_DC_FP05_MASK`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-05-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_bytes_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP05_MASK_PASS` / `COV_DC_FP05_MASK`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-06-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lag_ldc_ex1_inst_vld=1` 且 `lq_ldc_ex2_full=0` 时|则 `ldc_lq_ex2_create_vld=1` 且 `ldc_lda_ex2_lq_entry=lq_ldc_create_entry`|`CHK_DC_FP06_LQ_ACCEPT` / `COV_DC_FP06_LQ`；accept对应唯一entry，full或flush时不创建|
|`DC-FP-06-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lq_ex2_create_vld` 保持二态且只归属于同一owner|`CHK_DC_FP06_LQ_ACCEPT` / `COV_DC_FP06_LQ`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-06-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lq_ex2_create_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP06_LQ_ACCEPT` / `COV_DC_FP06_LQ`；winner唯一，loser payload零次误消费|
|`DC-FP-06-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lq_ex2_create_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP06_LQ_ACCEPT` / `COV_DC_FP06_LQ`；flush后的completion/create/forward计数均不增加|
|`DC-FP-06-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lq_ex2_create_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_lq_ex2_create_vld)=0`|`CHK_DC_FP06_LQ_ACCEPT` / `COV_DC_FP06_LQ`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-06-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lq_ex2_create_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP06_LQ_ACCEPT` / `COV_DC_FP06_LQ`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-07-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lq_ldc_ex2_full=1` 或 `mmu_lsu_tlb_busy=1` 命中live owner时|则 `ldc_lda_ex2_inst_vld=0` 且对应restart原因输出为1|`CHK_DC_FP07_RESTART_BLOCK` / `COV_DC_FP07_RESTART`；每个原因及两两交叉均只产生一次restart|
|`DC-FP-07-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_inst_vld` 保持二态且只归属于同一owner|`CHK_DC_FP07_RESTART_BLOCK` / `COV_DC_FP07_RESTART`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-07-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_inst_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP07_RESTART_BLOCK` / `COV_DC_FP07_RESTART`；winner唯一，loser payload零次误消费|
|`DC-FP-07-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_inst_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP07_RESTART_BLOCK` / `COV_DC_FP07_RESTART`；flush后的completion/create/forward计数均不增加|
|`DC-FP-07-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_inst_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_inst_vld)=0`|`CHK_DC_FP07_RESTART_BLOCK` / `COV_DC_FP07_RESTART`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-07-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_inst_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP07_RESTART_BLOCK` / `COV_DC_FP07_RESTART`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-08-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lag_ldc_ex1_expt_vld=1` 随live owner进入DC时|则 C1 `ldc_lda_ex2_expt_vld_except_access_err=1` 且异常owner不变|`CHK_DC_FP08_EXCEPTION` / `COV_DC_FP08_EXCEPTION`；PF/misalign/AF每个子类不丢失不重复|
|`DC-FP-08-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_expt_vld_except_access_err` 保持二态且只归属于同一owner|`CHK_DC_FP08_EXCEPTION` / `COV_DC_FP08_EXCEPTION`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-08-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_expt_vld_except_access_err` 只反映文档定义的winner且不含X|`CHK_DC_FP08_EXCEPTION` / `COV_DC_FP08_EXCEPTION`；winner唯一，loser payload零次误消费|
|`DC-FP-08-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_expt_vld_except_access_err` 不得产生被flush owner的新副作用|`CHK_DC_FP08_EXCEPTION` / `COV_DC_FP08_EXCEPTION`；flush后的completion/create/forward计数均不增加|
|`DC-FP-08-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_expt_vld_except_access_err` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_expt_vld_except_access_err)=0`|`CHK_DC_FP08_EXCEPTION` / `COV_DC_FP08_EXCEPTION`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-08-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_expt_vld_except_access_err` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP08_EXCEPTION` / `COV_DC_FP08_EXCEPTION`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-09-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `sq_ldc_ex2_fwd_req=1` 且 `wmb_ldc_fwd_req=0` 时|则 `ldc_lda_ex2_fwd_sq_vld=1`、`ldc_lda_ex2_fwd_wmb_vld=0` 且byte mask属于SQ owner|`CHK_DC_FP09_FWD_OWNER` / `COV_DC_FP09_FWD`；SQ/WMB互异花纹不得串source|
|`DC-FP-09-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_fwd_sq_vld` 保持二态且只归属于同一owner|`CHK_DC_FP09_FWD_OWNER` / `COV_DC_FP09_FWD`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-09-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_fwd_sq_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP09_FWD_OWNER` / `COV_DC_FP09_FWD`；winner唯一，loser payload零次误消费|
|`DC-FP-09-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_fwd_sq_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP09_FWD_OWNER` / `COV_DC_FP09_FWD`；flush后的completion/create/forward计数均不增加|
|`DC-FP-09-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_fwd_sq_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_fwd_sq_vld)=0`|`CHK_DC_FP09_FWD_OWNER` / `COV_DC_FP09_FWD`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-09-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_fwd_sq_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP09_FWD_OWNER` / `COV_DC_FP09_FWD`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-10-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lag_ldc_ex1_inst_vld=1` 被DC接受时|则 C1 `ldc_lda_ex2_inst_vld=1` 且IID、地址payload hash与原owner一致|`CHK_DC_FP10_DA_PAYLOAD` / `COV_DC_FP10_DA`；inst与borrow back-to-back时每次accept只转移一个owner|
|`DC-FP-10-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_inst_vld` 保持二态且只归属于同一owner|`CHK_DC_FP10_DA_PAYLOAD` / `COV_DC_FP10_DA`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-10-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_inst_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP10_DA_PAYLOAD` / `COV_DC_FP10_DA`；winner唯一，loser payload零次误消费|
|`DC-FP-10-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_inst_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP10_DA_PAYLOAD` / `COV_DC_FP10_DA`；flush后的completion/create/forward计数均不增加|
|`DC-FP-10-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_inst_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_inst_vld)=0`|`CHK_DC_FP10_DA_PAYLOAD` / `COV_DC_FP10_DA`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-10-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_inst_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP10_DA_PAYLOAD` / `COV_DC_FP10_DA`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-11-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ld_ag_dtu_vld=1` 随live load进入时|则 C1 `ld_dc_dtu_addr_vld=1` 且 `ld_dc_dtu_addr=ld_ag_dtu_va`，C2必须回到0|`CHK_DC_FP11_DTU_PULSE` / `COV_DC_FP11_DTU`；debug/normal/debug只产生两个单拍有效脉冲|
|`DC-FP-11-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ld_dc_dtu_addr_vld` 保持二态且只归属于同一owner|`CHK_DC_FP11_DTU_PULSE` / `COV_DC_FP11_DTU`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-11-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ld_dc_dtu_addr_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP11_DTU_PULSE` / `COV_DC_FP11_DTU`；winner唯一，loser payload零次误消费|
|`DC-FP-11-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ld_dc_dtu_addr_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP11_DTU_PULSE` / `COV_DC_FP11_DTU`；flush后的completion/create/forward计数均不增加|
|`DC-FP-11-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ld_dc_dtu_addr_vld` 仅在所属accept窗口有效且 `$isunknown(ld_dc_dtu_addr_vld)=0`|`CHK_DC_FP11_DTU_PULSE` / `COV_DC_FP11_DTU`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-11-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ld_dc_dtu_addr_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP11_DTU_PULSE` / `COV_DC_FP11_DTU`；scoreboard generation递增且旧响应零次命中新owner|
|`DC-FP-12-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `cp0_lsu_icg_en=0`、`pad_yy_icg_scan_en=1` 且owner有效时|则scan路径允许状态捕获并使 `ldc_lda_ex2_inst_vld=1`，reset低时所有valid为0|`CHK_DC_FP12_CLOCK_RESET` / `COV_DC_FP12_CLOCK`；ICG on/off/scan/reset边界均无X或幽灵owner|
|`DC-FP-12-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lag_ldc_ex1_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ldc_lda_ex2_inst_vld` 保持二态且只归属于同一owner|`CHK_DC_FP12_CLOCK_RESET` / `COV_DC_FP12_CLOCK`；两拍保持期间payload hash和owner均不漂移|
|`DC-FP-12-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lag_ldc_ex1_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ldc_lda_ex2_inst_vld` 只反映文档定义的winner且不含X|`CHK_DC_FP12_CLOCK_RESET` / `COV_DC_FP12_CLOCK`；winner唯一，loser payload零次误消费|
|`DC-FP-12-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lag_ldc_ex1_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `ldc_lda_ex2_inst_vld=0` 且 `ldc_lda_ex2_inst_vld` 不得产生被flush owner的新副作用|`CHK_DC_FP12_CLOCK_RESET` / `COV_DC_FP12_CLOCK`；flush后的completion/create/forward计数均不增加|
|`DC-FP-12-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lag_ldc_ex1_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ldc_lda_ex2_inst_vld` 仅在所属accept窗口有效且 `$isunknown(ldc_lda_ex2_inst_vld)=0`|`CHK_DC_FP12_CLOCK_RESET` / `COV_DC_FP12_CLOCK`；0/1/N延迟与零/最大值交叉全部命中|
|`DC-FP-12-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lag_ldc_ex1_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ldc_lda_ex2_inst_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DC_FP12_CLOCK_RESET` / `COV_DC_FP12_CLOCK`；scoreboard generation递增且旧响应零次命中新owner|

<!-- DC-INTERACTION-2.1-END -->
