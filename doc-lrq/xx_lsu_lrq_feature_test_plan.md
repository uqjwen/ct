# `xx_lsu_lrq` / `xx_lsu_lrq_entry` 功能点与 Test Plan

范围包含三个 LRQ bank 的 allocation/age/issue，以及 `xx_lsu_lrq_entry` 的事务 payload、freeze、wakeup、replay 和 flush 生命周期。scoreboard 使用 `{bank, entry, IID, observer generation}` 防止 entry 复用掩盖旧响应。

| 二级功能点 | 三级功能点 | 功能点描述 | 测试方法和配置说明 | 优先级 |
|---|---|---|---|---|
| 容量管理 | free pointer | 每个 bank 依据 entry valid 生成 create pointer、full/less 条件和 no-space 反馈；allocation 区见 `srcs/xx_lsu_lrq.sv:1411`～`1470` | empty/one-left/full，三 bank 同拍 create/pop；断言 accepted create 指针 onehot且互不重叠 | P0 |
| Create | accept/pop 一致 | AG raw create 通过 flush/full资格后建立 entry，同时只弹出对应上游事务；见 `srcs/xx_lsu_lrq.sv:1448` | create 与 full/check flush 同拍，覆盖 raw/DP/function valid；检查失败只因 flush kill，非 flush 不得 raw pop without accept | P0 |
| Entry payload | fresh 元数据保存 | `xx_lsu_lrq_entry` 保存 VA、IID、mask、vector、debug、split 等 replay 字段；模块见 `srcs/xx_lsu_lrq_entry.sv:25` | 每字段使用互异花纹，fresh create 后等待 N 拍再 replay；适用 payload 与 create 值逐 bit 相同 | P0 |
| Freeze | 等待原因集合 | MMU、barrier、no-spec、old check、LFB/SQ/WMB 等原因共同决定 ready；entry wake 逻辑见 `srcs/xx_lsu_lrq_entry.sv:817` | 每种原因单独与两两组合，逐一解除；只有所有必要条件解除后才能 issue | P0 |
| Wakeup | producer owner | MMU/LFB/SQ/WMB bitmap 只能唤醒仍有效且 IID owner 匹配的 entry；汇总见 `srcs/xx_lsu_lrq.sv:1692` | live、已释放、flush-killed、立即复用四类 bit 注入 wake；旧 owner wake 必须触发 assertion | P0 |
| Issue | oldest ready 仲裁 | 各 bank 从 ready entry 中选择年龄最老者并形成 RF replay；age/issue 区见 `srcs/xx_lsu_lrq.sv:1628`～`1683` | 不同 IID 排列和 wrap，多 ready 同拍；软件 age model 对比 winner且 grant onehot | P0 |
| Replay | RF mux | LRQ entry payload经 replay bus回到 AG，不能再次创建 LRQ；mux 区见 `srcs/xx_lsu_lrq.sv:1786`～`1822` | scalar/boundary/vector/US create→replay；断言 replay accept 时 create_vld 为 0，payload无当前 IDU 污染 | P0 |
| No-spec/Barrier | 前序依赖 | wait-old、barrier、no-spec 只在对应前序条件消失后解冻；状态输出见 `srcs/xx_lsu_lrq.sv:1792` | 多 bank 年龄交叉、前序请求逐拍完成；检查无过早 issue、无永久 freeze和 age 环 | P1 |
| DA 反馈 | secd/already/spec fail | DA 对 live entry 更新 second-part、already-DA、spec-fail与 pop；接口状态见 `srcs/xx_lsu_lrq.sv:204` | 反馈与 flush/pop/create 同拍，entry 随即复用；generation scoreboard 检查旧反馈零次修改新 owner | P0 |
| Flush | full/check 清除 | full flush 清全部，check flush按 IID年龄清年轻 entry并阻止同拍复用；flush 输入见 `srcs/xx_lsu_lrq.sv:40`～`42` | IID older/equal/newer和 wrap，覆盖 create/wakeup/issue 各边；检查 killed entry无迟到 replay | P0 |
| Clock/Reset | entry 数据门控 | entry valid、create和反馈状态更新必须打开相应数据钟；entry clock逻辑见 `srcs/xx_lsu_lrq_entry.sv:227` | ICG on/off/scan，gate-only DP、reset 释放；检查功能 valid 高时 payload实际锁存且清除边沿不丢 | P1 |
| 参数合同 | LRQENTRY/LSIQENTRY | bitmap wakeup和 issue queue隐含相同 entry 数/编码合同；参数见 `srcs/xx_lsu_lrq.sv:29`～`33` | 正式参数 elaboration通过；故意配置不等宽时静态 assertion明确失败而非静默截断 | P2 |

签核必须同时观察 `xx_lsu_lrq` 与 `xx_lsu_lrq_entry`；只看 replay 最终完成无法识别旧 owner wakeup命中新 entry。

<!-- LRQ-INTERACTION-2.1-BEGIN -->

## Interaction 2.1 叶级场景与执行合同

共72行；每个父功能点6行。状态只允许 `BLOCKED_NO_VCS` 或 `PENDING_FULL_CHIP`。
- create_vld=1只表示raw请求；与flush交叉时必须记录create_success=0。
- Scoreboard按 `{bank, entry, IID, generation}` 跟踪旧 owner、entry复用和所有wakeup来源。

|场景|setup/周期|当|则|checker/coverage/关闭|
|---|---|---|---|---|
|`LRQ-FP-01-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当三bank `lsu0_lrq_create_vld=1` 等create请求到达时；触发信号 `lsu0_lrq_create_vld`|则每个被接受bank只选择一个one-hot LRQ entry，no-space bank不建立owner；交付信号 `lrq_lsu0_ex1_lrqid`|`CHK_LRQ_FP01_CAPACITY` / `COV_LRQ_FP01_CAPACITY`；empty、one-left、full和同拍pop以{bank,entry,IID,generation}计数|
|`LRQ-FP-01-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_ex1_lrqid` 保持二态且只归属于同一owner|`CHK_LRQ_FP01_CAPACITY` / `COV_LRQ_FP01_CAPACITY`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-01-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_ex1_lrqid` 只反映文档定义的winner且不含X|`CHK_LRQ_FP01_CAPACITY` / `COV_LRQ_FP01_CAPACITY`；winner唯一，loser payload零次误消费|
|`LRQ-FP-01-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_ex1_lrqid` 不得产生被flush owner的新副作用|`CHK_LRQ_FP01_CAPACITY` / `COV_LRQ_FP01_CAPACITY`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-01-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_ex1_lrqid` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_ex1_lrqid)=0`|`CHK_LRQ_FP01_CAPACITY` / `COV_LRQ_FP01_CAPACITY`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-01-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_ex1_lrqid` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP01_CAPACITY` / `COV_LRQ_FP01_CAPACITY`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-02-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lsu0_lrq_create_vld=1` 与full或flush边界交叉时；触发信号 `lsu0_lrq_create_vld`|则仅合格create建立entry；flush取消时create_success=0且不产生raw-pop副作用；交付信号 `lrq_lsu0_rf_replay_vld`|`CHK_LRQ_FP02_CREATE_ACCEPT` / `COV_LRQ_FP02_CREATE`；create_vld=1不等于成功，失败原因必须可归因于flush或no-space|
|`LRQ-FP-02-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_replay_vld` 保持二态且只归属于同一owner|`CHK_LRQ_FP02_CREATE_ACCEPT` / `COV_LRQ_FP02_CREATE`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-02-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_replay_vld` 只反映文档定义的winner且不含X|`CHK_LRQ_FP02_CREATE_ACCEPT` / `COV_LRQ_FP02_CREATE`；winner唯一，loser payload零次误消费|
|`LRQ-FP-02-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_replay_vld` 不得产生被flush owner的新副作用|`CHK_LRQ_FP02_CREATE_ACCEPT` / `COV_LRQ_FP02_CREATE`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-02-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_replay_vld` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_replay_vld)=0`|`CHK_LRQ_FP02_CREATE_ACCEPT` / `COV_LRQ_FP02_CREATE`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-02-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_replay_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP02_CREATE_ACCEPT` / `COV_LRQ_FP02_CREATE`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-03-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当fresh `lsu0_lrq_create_vld=1` 被接受并等待N拍后replay时；触发信号 `lsu0_lrq_create_vld`|则VA、IID、mask及适用vector字段逐bit等于create payload；交付信号 `lrq_lsu0_rf_va`|`CHK_LRQ_FP03_PAYLOAD` / `COV_LRQ_FP03_PAYLOAD`；所有字段使用互异花纹，等待期间owner payload稳定|
|`LRQ-FP-03-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_va` 保持二态且只归属于同一owner|`CHK_LRQ_FP03_PAYLOAD` / `COV_LRQ_FP03_PAYLOAD`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-03-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_va` 只反映文档定义的winner且不含X|`CHK_LRQ_FP03_PAYLOAD` / `COV_LRQ_FP03_PAYLOAD`；winner唯一，loser payload零次误消费|
|`LRQ-FP-03-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_va` 不得产生被flush owner的新副作用|`CHK_LRQ_FP03_PAYLOAD` / `COV_LRQ_FP03_PAYLOAD`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-03-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_va` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_va)=0`|`CHK_LRQ_FP03_PAYLOAD` / `COV_LRQ_FP03_PAYLOAD`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-03-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_va` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP03_PAYLOAD` / `COV_LRQ_FP03_PAYLOAD`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-04-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lsu0_lrq_create_frz=1` 或MMU、barrier、no-spec、LQ/SQ/RB原因仍有效时；触发信号 `lsu0_lrq_create_frz`|则entry不得issue；只有全部必要原因解除后 `lrq_lsu0_rf_replay_vld=1`；交付信号 `lrq_lsu0_rf_replay_vld`|`CHK_LRQ_FP04_FREEZE` / `COV_LRQ_FP04_FREEZE`；每种freeze原因单独及两两组合，解除顺序不改变owner|
|`LRQ-FP-04-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_frz=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_replay_vld` 保持二态且只归属于同一owner|`CHK_LRQ_FP04_FREEZE` / `COV_LRQ_FP04_FREEZE`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-04-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_frz=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_replay_vld` 只反映文档定义的winner且不含X|`CHK_LRQ_FP04_FREEZE` / `COV_LRQ_FP04_FREEZE`；winner唯一，loser payload零次误消费|
|`LRQ-FP-04-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_frz=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_replay_vld` 不得产生被flush owner的新副作用|`CHK_LRQ_FP04_FREEZE` / `COV_LRQ_FP04_FREEZE`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-04-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_frz=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_replay_vld` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_replay_vld)=0`|`CHK_LRQ_FP04_FREEZE` / `COV_LRQ_FP04_FREEZE`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-04-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_frz=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_replay_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP04_FREEZE` / `COV_LRQ_FP04_FREEZE`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-05-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当MMU/LFB/SQ/WMB wakeup bitmap命中LRQ bit时；触发信号 `lsu0_lrq_exx_tlb_wakeup`|则wakeup只作用于仍valid且generation匹配的entry；旧 owner wakeup不得修改entry复用后的新owner；交付信号 `lrq0_idu_exx_wakeup`|`CHK_LRQ_FP05_WAKEUP_OWNER` / `COV_LRQ_FP05_WAKEUP`；live、killed、已释放和立即复用四类bit均覆盖|
|`LRQ-FP-05-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_exx_tlb_wakeup=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq0_idu_exx_wakeup` 保持二态且只归属于同一owner|`CHK_LRQ_FP05_WAKEUP_OWNER` / `COV_LRQ_FP05_WAKEUP`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-05-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_exx_tlb_wakeup=1` 与同级竞争条件同拍成立时|则 C1 `lrq0_idu_exx_wakeup` 只反映文档定义的winner且不含X|`CHK_LRQ_FP05_WAKEUP_OWNER` / `COV_LRQ_FP05_WAKEUP`；winner唯一，loser payload零次误消费|
|`LRQ-FP-05-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_exx_tlb_wakeup=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq0_idu_exx_wakeup` 不得产生被flush owner的新副作用|`CHK_LRQ_FP05_WAKEUP_OWNER` / `COV_LRQ_FP05_WAKEUP`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-05-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_exx_tlb_wakeup=1` 且目标payload取边界值时|则 C1至C3 `lrq0_idu_exx_wakeup` 仅在所属accept窗口有效且 `$isunknown(lrq0_idu_exx_wakeup)=0`|`CHK_LRQ_FP05_WAKEUP_OWNER` / `COV_LRQ_FP05_WAKEUP`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-05-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_exx_tlb_wakeup=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq0_idu_exx_wakeup` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP05_WAKEUP_OWNER` / `COV_LRQ_FP05_WAKEUP`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-06-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当多个ready entry竞争且 `idu_lsu_old_vld=1` 时；触发信号 `idu_lsu_old_vld`|则软件age模型认定的最老IID成为唯一replay winner；交付信号 `lrq_lsu0_rf_replay_vld`|`CHK_LRQ_FP06_OLDEST` / `COV_LRQ_FP06_OLDEST`；普通排序、IID wrap、equal边界和三bank同拍ready均检查one-hot grant|
|`LRQ-FP-06-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `idu_lsu_old_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_replay_vld` 保持二态且只归属于同一owner|`CHK_LRQ_FP06_OLDEST` / `COV_LRQ_FP06_OLDEST`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-06-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `idu_lsu_old_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_replay_vld` 只反映文档定义的winner且不含X|`CHK_LRQ_FP06_OLDEST` / `COV_LRQ_FP06_OLDEST`；winner唯一，loser payload零次误消费|
|`LRQ-FP-06-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `idu_lsu_old_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_replay_vld` 不得产生被flush owner的新副作用|`CHK_LRQ_FP06_OLDEST` / `COV_LRQ_FP06_OLDEST`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-06-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `idu_lsu_old_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_replay_vld` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_replay_vld)=0`|`CHK_LRQ_FP06_OLDEST` / `COV_LRQ_FP06_OLDEST`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-06-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `idu_lsu_old_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_replay_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP06_OLDEST` / `COV_LRQ_FP06_OLDEST`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-07-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当保存的scalar、boundary、vector或US owner被选择replay时；触发信号 `lsu0_lrq_create_vld`|则RF mux payload属于该entry，replay拍不得再次建立LRQ create；交付信号 `lrq_lsu0_rf_replay_vld`|`CHK_LRQ_FP07_REPLAY` / `COV_LRQ_FP07_REPLAY`；replay payload不受当前IDU互异输入污染且每个owner只issue一次|
|`LRQ-FP-07-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_replay_vld` 保持二态且只归属于同一owner|`CHK_LRQ_FP07_REPLAY` / `COV_LRQ_FP07_REPLAY`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-07-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_replay_vld` 只反映文档定义的winner且不含X|`CHK_LRQ_FP07_REPLAY` / `COV_LRQ_FP07_REPLAY`；winner唯一，loser payload零次误消费|
|`LRQ-FP-07-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_replay_vld` 不得产生被flush owner的新副作用|`CHK_LRQ_FP07_REPLAY` / `COV_LRQ_FP07_REPLAY`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-07-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_replay_vld` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_replay_vld)=0`|`CHK_LRQ_FP07_REPLAY` / `COV_LRQ_FP07_REPLAY`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-07-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_replay_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP07_REPLAY` / `COV_LRQ_FP07_REPLAY`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-08-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当entry带no-spec或barrier检查且前序条件仍存在时；触发信号 `lsu0_lrq_create_no_spec_chk`|则不得过早replay；前序条件消失后唯一owner解除freeze；交付信号 `lrq0_hit_no_spec_tbl`|`CHK_LRQ_FP08_BARRIER` / `COV_LRQ_FP08_BARRIER`；多bank年龄交叉、逐拍释放和无永久freeze均覆盖|
|`LRQ-FP-08-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_no_spec_chk=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq0_hit_no_spec_tbl` 保持二态且只归属于同一owner|`CHK_LRQ_FP08_BARRIER` / `COV_LRQ_FP08_BARRIER`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-08-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_no_spec_chk=1` 与同级竞争条件同拍成立时|则 C1 `lrq0_hit_no_spec_tbl` 只反映文档定义的winner且不含X|`CHK_LRQ_FP08_BARRIER` / `COV_LRQ_FP08_BARRIER`；winner唯一，loser payload零次误消费|
|`LRQ-FP-08-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_no_spec_chk=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq0_hit_no_spec_tbl` 不得产生被flush owner的新副作用|`CHK_LRQ_FP08_BARRIER` / `COV_LRQ_FP08_BARRIER`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-08-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_no_spec_chk=1` 且目标payload取边界值时|则 C1至C3 `lrq0_hit_no_spec_tbl` 仅在所属accept窗口有效且 `$isunknown(lrq0_hit_no_spec_tbl)=0`|`CHK_LRQ_FP08_BARRIER` / `COV_LRQ_FP08_BARRIER`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-08-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_no_spec_chk=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq0_hit_no_spec_tbl` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP08_BARRIER` / `COV_LRQ_FP08_BARRIER`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-09-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当DA的secd、already-DA、spec-fail或pop反馈命中live bit时；触发信号 `lsu0_lrq_ex3_secd`|则只更新匹配generation的owner；旧反馈零次修改entry复用后的新owner；交付信号 `lrq_lsu0_rf_already_da`|`CHK_LRQ_FP09_DA_FEEDBACK` / `COV_LRQ_FP09_DA`；反馈与flush、pop、create同拍时按{bank,entry,IID,generation}判定|
|`LRQ-FP-09-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_ex3_secd=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_already_da` 保持二态且只归属于同一owner|`CHK_LRQ_FP09_DA_FEEDBACK` / `COV_LRQ_FP09_DA`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-09-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_ex3_secd=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_already_da` 只反映文档定义的winner且不含X|`CHK_LRQ_FP09_DA_FEEDBACK` / `COV_LRQ_FP09_DA`；winner唯一，loser payload零次误消费|
|`LRQ-FP-09-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_ex3_secd=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_already_da` 不得产生被flush owner的新副作用|`CHK_LRQ_FP09_DA_FEEDBACK` / `COV_LRQ_FP09_DA`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-09-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_ex3_secd=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_already_da` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_already_da)=0`|`CHK_LRQ_FP09_DA_FEEDBACK` / `COV_LRQ_FP09_DA`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-09-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_ex3_secd=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_already_da` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP09_DA_FEEDBACK` / `COV_LRQ_FP09_DA`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-10-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当full flush或check flush命中live entry时；触发信号 `lsu0_lrq_create_vld`|则full flush清全部，check flush仅清更年轻未提交owner，killed entry不再replay或wakeup；交付信号 `lrq_lsu0_rf_replay_vld`|`CHK_LRQ_FP10_FLUSH` / `COV_LRQ_FP10_FLUSH`；IID older/equal/newer及wrap与create/wakeup/issue边界交叉|
|`LRQ-FP-10-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_replay_vld` 保持二态且只归属于同一owner|`CHK_LRQ_FP10_FLUSH` / `COV_LRQ_FP10_FLUSH`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-10-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_replay_vld` 只反映文档定义的winner且不含X|`CHK_LRQ_FP10_FLUSH` / `COV_LRQ_FP10_FLUSH`；winner唯一，loser payload零次误消费|
|`LRQ-FP-10-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_replay_vld` 不得产生被flush owner的新副作用|`CHK_LRQ_FP10_FLUSH` / `COV_LRQ_FP10_FLUSH`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-10-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_replay_vld` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_replay_vld)=0`|`CHK_LRQ_FP10_FLUSH` / `COV_LRQ_FP10_FLUSH`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-10-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_replay_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP10_FLUSH` / `COV_LRQ_FP10_FLUSH`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-11-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当ICG关闭、scan开启或reset命中entry更新边界时；触发信号 `lsu0_lrq_create_vld`|则功能valid打开相应数据钟，reset后valid与replay为0；交付信号 `lrq_lsu0_rf_replay_vld`|`CHK_LRQ_FP11_CLOCK_RESET` / `COV_LRQ_FP11_CLOCK`；valid gate与data gate一致，reset释放首拍无幽灵owner|
|`LRQ-FP-11-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_rf_replay_vld` 保持二态且只归属于同一owner|`CHK_LRQ_FP11_CLOCK_RESET` / `COV_LRQ_FP11_CLOCK`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-11-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_rf_replay_vld` 只反映文档定义的winner且不含X|`CHK_LRQ_FP11_CLOCK_RESET` / `COV_LRQ_FP11_CLOCK`；winner唯一，loser payload零次误消费|
|`LRQ-FP-11-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_rf_replay_vld` 不得产生被flush owner的新副作用|`CHK_LRQ_FP11_CLOCK_RESET` / `COV_LRQ_FP11_CLOCK`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-11-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_rf_replay_vld` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_rf_replay_vld)=0`|`CHK_LRQ_FP11_CLOCK_RESET` / `COV_LRQ_FP11_CLOCK`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-11-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_rf_replay_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP11_CLOCK_RESET` / `COV_LRQ_FP11_CLOCK`；scoreboard generation递增且旧响应零次命中新owner|
|`LRQ-FP-12-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当正式配置下 `lsu0_lrq_create_vld=1` 并使用LRQ/LSIQ bitmap时；触发信号 `lsu0_lrq_create_vld`|则 `LRQENTRY=LSIQENTRY` 的elaboration合同成立且bitmap无静默截断；交付信号 `lrq_lsu0_ex1_lrqid`|`CHK_LRQ_FP12_PARAMETER` / `COV_LRQ_FP12_PARAMETER`；正式相等配置必须通过；故意不等宽配置要求静态assert明确失败|
|`LRQ-FP-12-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lsu0_lrq_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lrq_lsu0_ex1_lrqid` 保持二态且只归属于同一owner|`CHK_LRQ_FP12_PARAMETER` / `COV_LRQ_FP12_PARAMETER`；两拍保持期间payload hash和owner均不漂移|
|`LRQ-FP-12-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lsu0_lrq_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lrq_lsu0_ex1_lrqid` 只反映文档定义的winner且不含X|`CHK_LRQ_FP12_PARAMETER` / `COV_LRQ_FP12_PARAMETER`；winner唯一，loser payload零次误消费|
|`LRQ-FP-12-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lsu0_lrq_create_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lrq_lsu0_rf_replay_vld=0` 且 `lrq_lsu0_ex1_lrqid` 不得产生被flush owner的新副作用|`CHK_LRQ_FP12_PARAMETER` / `COV_LRQ_FP12_PARAMETER`；flush后的completion/create/forward计数均不增加|
|`LRQ-FP-12-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lsu0_lrq_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lrq_lsu0_ex1_lrqid` 仅在所属accept窗口有效且 `$isunknown(lrq_lsu0_ex1_lrqid)=0`|`CHK_LRQ_FP12_PARAMETER` / `COV_LRQ_FP12_PARAMETER`；0/1/N延迟与零/最大值交叉全部命中|
|`LRQ-FP-12-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lsu0_lrq_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lrq_lsu0_ex1_lrqid` 只对应owner B，旧owner不得迟到修改|`CHK_LRQ_FP12_PARAMETER` / `COV_LRQ_FP12_PARAMETER`；scoreboard generation递增且旧响应零次命中新owner|

<!-- LRQ-INTERACTION-2.1-END -->
