# `xx_lsu_lfb` / `xx_lsu_lfb_addr_entry` 功能点与 Test Plan

范围包含 LFB 地址/数据项分配、RB/PFU create、BIU linefill response、VB replacement、D-cache refill、依赖唤醒和回收。仓库提供 `xx_lsu_lfb_addr_entry`，但 `xx_lsu_lfb` 所实例化的 `xx_lsu_lfb_data_entry` 源码缺失；补齐该模块并完成 elaboration 是执行以下 data-entry 测试前的硬性前置条件。

| 二级功能点 | 三级功能点 | 功能点描述 | 测试方法和配置说明 | 优先级 |
|---|---|---|---|---|
| 地址项分配 | RB/PFU create | RB和PFU请求选择空闲地址项并返回 create ID，entry保存 line地址与来源；入口见 `srcs/xx_lsu_lfb.sv:68`～`84` | empty/one-left/full，RB/PFU同拍竞争和互异地址；断言 success ID唯一、winner payload正确 | P0 |
| Entry 生命周期 | valid/create/pop | `xx_lsu_lfb_addr_entry` 在create置 valid，在linefill或data pop时释放；见 `srcs/xx_lsu_lfb_addr_entry.sv:230`～`239`、`499` | create、response、pop、立即复用同拍交叉；generation scoreboard检查旧事件不清新 owner | P0 |
| 地址命中 | request merge/hit | live地址项比较 RB/PFU/WMB/SNQ请求的 cache line，阻止重复 outstanding或支持bypass；见 `srcs/xx_lsu_lfb_addr_entry.sv:531`～`566` | same/different line、地址边界和多 entry匹配负向场景；正式场景命中 onehot0且 source一致 | P0 |
| 数据项分配 | address/data绑定 | BIU response按 ID绑定地址项和可用数据项，保存 beat/data/share/last；数据指针见 `srcs/xx_lsu_lfb.sv:192`～`203` | 两数据项 empty/one-left/full，交叉两地址项响应；检查 addr ID、data ID和payload不串项 | P0 |
| BIU R通道 | ID/beat/response | 根据 `biu_lsu_r_id/r_last/r_resp/r_user` 接收linefill数据并处理错误；接口见 `srcs/xx_lsu_lfb.sv:42`～`47` | 正常beat序列、错 ID、early/late last、response error、随机背压；协议错误触发assertion | P0 |
| VB 协同 | replacement完成 | 地址项申请VB选择refill way并保存hit/dirty/rcl_done；entry逻辑见 `srcs/xx_lsu_lfb_addr_entry.sv:289`～`325` | VB grant 0/1/N拍、hit clean/dirty/miss和多请求竞争；检查选择结果属于原地址项 | P0 |
| Refill | D-cache tag/data写 | linefill状态机按way把数据/ECC、tag、dirty写回D-cache；数据输出见 `srcs/xx_lsu_lfb.sv:1506`～`1513` | 四 way、所有data word、互异花纹、D-cache随机grant；逐 bit对比line reference model | P0 |
| Response完成 | all response/NC empty | 每地址项记录response，汇总all_resp及NC empty给flush/总线控制；见 `srcs/xx_lsu_lfb.sv:1653`～`1656` | 混合CA/NC项、乱序响应和error，检查all_resp只在全部live项完成后有效 | P1 |
| 依赖队列 | LRQ/LSIQ wakeup | load miss把依赖bitmap并入队列，refill/pop/flush产生一次wake并清除；见 `srcs/xx_lsu_lfb.sv:1522`～`1605` | 多依赖merge、部分/full flush、bit立即复用和负向旧wake；owner assertion检查零串项 | P0 |
| SNQ bypass | share/data ID | 已响应line可向SNQ提供bypass数据和share属性；逻辑见 `srcs/xx_lsu_lfb.sv:1724`～`1728` | 两data项互异花纹，SNQ同/不同line和多hit负向场景；检查data ID、share和有效期 | P1 |
| Flush | 队列与outstanding | full/check flush清依赖队列，但已发总线事务仍须安全回收且不唤醒被杀owner；清除见 `srcs/xx_lsu_lfb.sv:1547`～`1552` | 在create、VB、R beat、refill、pop各点flush；响应继续/停止两种模型检查无迟到功能副作用 | P0 |
| 容量/空闲 | full/less2/empty | 地址项和数据项分别生成full、less2、empty并参与系统反压；汇总见 `srcs/xx_lsu_lfb.sv:1730`～`1739` | fill→full→drain全状态，create/pop同拍和长期背压；reference counter逐拍一致 | P0 |
| Clock/Reset | entry/state门控 | entry valid/create、VB结果、response和wakeup队列更新需对应gateclk；entry clock见 `srcs/xx_lsu_lfb_addr_entry.sv:189`～`223` | ICG on/off/scan、孤立一拍事件、reset释放；检查实际clock pulse和寄存状态变化 | P1 |

LFB动态签核需把地址项、数据项和BIU/VB/D-cache三侧同时纳入scoreboard；仅验证最终cache line数据不能发现ID串项或被flush依赖的迟到唤醒。

<!-- LFB-INTERACTION-2.1-BEGIN -->

## Interaction 2.1 叶级场景与执行合同

共78行；每个父功能点6行。状态只允许 `BLOCKED_NO_VCS` 或 `PENDING_FULL_CHIP`。
- Scoreboard同时跟踪 `{addr entry, data entry, source ID, generation}`，两data entry及所有refill block使用互异花纹。
- 仓库缺少生产 `srcs/xx_lsu_lfb_data_entry.sv`；standalone兼容模型仅为 `PENDING_FULL_CHIP`，不得作为动态签核证据。

|场景|setup/周期|当|则|checker/coverage/关闭|
|---|---|---|---|---|
|`LFB-FP-01-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `rb_lfb_create_vld=1` 或PFU create请求到达且有空闲地址项时；触发信号 `rb_lfb_create_vld`|则winner获得唯一create ID，返回ID与来源地址属于同一owner；交付信号 `lfb_rb_create_id`|`CHK_LFB_FP01_ALLOCATE` / `COV_LFB_FP01_ALLOCATE`；empty、one-left、full及RB/PFU同拍竞争使用互异地址|
|`LFB-FP-01-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lfb_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_rb_create_id` 保持二态且只归属于同一owner|`CHK_LFB_FP01_ALLOCATE` / `COV_LFB_FP01_ALLOCATE`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-01-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lfb_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lfb_rb_create_id` 只反映文档定义的winner且不含X|`CHK_LFB_FP01_ALLOCATE` / `COV_LFB_FP01_ALLOCATE`；winner唯一，loser payload零次误消费|
|`LFB-FP-01-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lfb_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_rb_create_id` 不得产生被flush owner的新副作用|`CHK_LFB_FP01_ALLOCATE` / `COV_LFB_FP01_ALLOCATE`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-01-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lfb_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lfb_rb_create_id` 仅在所属accept窗口有效且 `$isunknown(lfb_rb_create_id)=0`|`CHK_LFB_FP01_ALLOCATE` / `COV_LFB_FP01_ALLOCATE`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-01-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lfb_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_rb_create_id` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP01_ALLOCATE` / `COV_LFB_FP01_ALLOCATE`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-02-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当地址entry依次经历create、response和pop时；触发信号 `rb_lfb_create_vld`|则valid按生命周期置位和释放，立即复用时旧事件不清除新owner；交付信号 `lsu_had_lfb_addr_entry_vld`|`CHK_LFB_FP02_ADDR_LIFECYCLE` / `COV_LFB_FP02_ADDR`；scoreboard记录{addr entry,generation,source ID,data ID}|
|`LFB-FP-02-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lfb_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_had_lfb_addr_entry_vld` 保持二态且只归属于同一owner|`CHK_LFB_FP02_ADDR_LIFECYCLE` / `COV_LFB_FP02_ADDR`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-02-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lfb_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lsu_had_lfb_addr_entry_vld` 只反映文档定义的winner且不含X|`CHK_LFB_FP02_ADDR_LIFECYCLE` / `COV_LFB_FP02_ADDR`；winner唯一，loser payload零次误消费|
|`LFB-FP-02-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lfb_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lsu_had_lfb_addr_entry_vld` 不得产生被flush owner的新副作用|`CHK_LFB_FP02_ADDR_LIFECYCLE` / `COV_LFB_FP02_ADDR`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-02-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lfb_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lsu_had_lfb_addr_entry_vld` 仅在所属accept窗口有效且 `$isunknown(lsu_had_lfb_addr_entry_vld)=0`|`CHK_LFB_FP02_ADDR_LIFECYCLE` / `COV_LFB_FP02_ADDR`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-02-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lfb_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_had_lfb_addr_entry_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP02_ADDR_LIFECYCLE` / `COV_LFB_FP02_ADDR`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-03-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当请求地址与live LFB cache line相同或不同时；触发信号 `rb_lfb_create_req`|则对应source hit满足onehot0且不同line不误命中；交付信号 `lfb_rb_biu_req_hit_idx`|`CHK_LFB_FP03_ADDRESS_HIT` / `COV_LFB_FP03_HIT`；same/different line、边界、多entry匹配负向场景均覆盖|
|`LFB-FP-03-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lfb_create_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_rb_biu_req_hit_idx` 保持二态且只归属于同一owner|`CHK_LFB_FP03_ADDRESS_HIT` / `COV_LFB_FP03_HIT`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-03-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lfb_create_req=1` 与同级竞争条件同拍成立时|则 C1 `lfb_rb_biu_req_hit_idx` 只反映文档定义的winner且不含X|`CHK_LFB_FP03_ADDRESS_HIT` / `COV_LFB_FP03_HIT`；winner唯一，loser payload零次误消费|
|`LFB-FP-03-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lfb_create_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_rb_biu_req_hit_idx` 不得产生被flush owner的新副作用|`CHK_LFB_FP03_ADDRESS_HIT` / `COV_LFB_FP03_HIT`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-03-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lfb_create_req=1` 且目标payload取边界值时|则 C1至C3 `lfb_rb_biu_req_hit_idx` 仅在所属accept窗口有效且 `$isunknown(lfb_rb_biu_req_hit_idx)=0`|`CHK_LFB_FP03_ADDRESS_HIT` / `COV_LFB_FP03_HIT`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-03-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lfb_create_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_rb_biu_req_hit_idx` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP03_ADDRESS_HIT` / `COV_LFB_FP03_HIT`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-04-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当BIU response按ID命中地址项且存在空闲数据项时；触发信号 `biu_lsu_r_vld`|则addr ID、data ID与两数据项互异payload保持同一owner绑定；交付信号 `lsu_had_lfb_data_entry_vld`|`CHK_LFB_FP04_DATA_BINDING` / `COV_LFB_FP04_DATA`；data entry empty、one-left、full和交叉两地址响应均不串项|
|`LFB-FP-04-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `biu_lsu_r_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_had_lfb_data_entry_vld` 保持二态且只归属于同一owner|`CHK_LFB_FP04_DATA_BINDING` / `COV_LFB_FP04_DATA`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-04-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `biu_lsu_r_vld=1` 与同级竞争条件同拍成立时|则 C1 `lsu_had_lfb_data_entry_vld` 只反映文档定义的winner且不含X|`CHK_LFB_FP04_DATA_BINDING` / `COV_LFB_FP04_DATA`；winner唯一，loser payload零次误消费|
|`LFB-FP-04-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `biu_lsu_r_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lsu_had_lfb_data_entry_vld` 不得产生被flush owner的新副作用|`CHK_LFB_FP04_DATA_BINDING` / `COV_LFB_FP04_DATA`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-04-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `biu_lsu_r_vld=1` 且目标payload取边界值时|则 C1至C3 `lsu_had_lfb_data_entry_vld` 仅在所属accept窗口有效且 `$isunknown(lsu_had_lfb_data_entry_vld)=0`|`CHK_LFB_FP04_DATA_BINDING` / `COV_LFB_FP04_DATA`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-04-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `biu_lsu_r_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_had_lfb_data_entry_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP04_DATA_BINDING` / `COV_LFB_FP04_DATA`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-05-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当匹配ID的BIU R beat到达时；触发信号 `biu_lsu_r_vld`|则ready/beat/last严格对应原owner；错ID、early/late last或error不得静默完成；交付信号 `lsu_biu_r_linefill_ready`|`CHK_LFB_FP05_BIU_RESPONSE` / `COV_LFB_FP05_BIU`；正常序列、错误响应和随机背压以beat计数器逐拍检查|
|`LFB-FP-05-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `biu_lsu_r_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_biu_r_linefill_ready` 保持二态且只归属于同一owner|`CHK_LFB_FP05_BIU_RESPONSE` / `COV_LFB_FP05_BIU`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-05-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `biu_lsu_r_vld=1` 与同级竞争条件同拍成立时|则 C1 `lsu_biu_r_linefill_ready` 只反映文档定义的winner且不含X|`CHK_LFB_FP05_BIU_RESPONSE` / `COV_LFB_FP05_BIU`；winner唯一，loser payload零次误消费|
|`LFB-FP-05-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `biu_lsu_r_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lsu_biu_r_linefill_ready` 不得产生被flush owner的新副作用|`CHK_LFB_FP05_BIU_RESPONSE` / `COV_LFB_FP05_BIU`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-05-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `biu_lsu_r_vld=1` 且目标payload取边界值时|则 C1至C3 `lsu_biu_r_linefill_ready` 仅在所属accept窗口有效且 `$isunknown(lsu_biu_r_linefill_ready)=0`|`CHK_LFB_FP05_BIU_RESPONSE` / `COV_LFB_FP05_BIU`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-05-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `biu_lsu_r_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_biu_r_linefill_ready` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP05_BIU_RESPONSE` / `COV_LFB_FP05_BIU`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-06-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当地址项申请VB并在0/1/N拍后获得grant时；触发信号 `vb_lfb_create_grnt`|则refill way、hit/dirty和VB ID只归属于原地址entry；交付信号 `lfb_vb_create_vld`|`CHK_LFB_FP06_VB` / `COV_LFB_FP06_VB`；hit clean、hit dirty、miss和多entry竞争全部覆盖|
|`LFB-FP-06-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `vb_lfb_create_grnt=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_vb_create_vld` 保持二态且只归属于同一owner|`CHK_LFB_FP06_VB` / `COV_LFB_FP06_VB`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-06-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `vb_lfb_create_grnt=1` 与同级竞争条件同拍成立时|则 C1 `lfb_vb_create_vld` 只反映文档定义的winner且不含X|`CHK_LFB_FP06_VB` / `COV_LFB_FP06_VB`；winner唯一，loser payload零次误消费|
|`LFB-FP-06-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `vb_lfb_create_grnt=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_vb_create_vld` 不得产生被flush owner的新副作用|`CHK_LFB_FP06_VB` / `COV_LFB_FP06_VB`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-06-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `vb_lfb_create_grnt=1` 且目标payload取边界值时|则 C1至C3 `lfb_vb_create_vld` 仅在所属accept窗口有效且 `$isunknown(lfb_vb_create_vld)=0`|`CHK_LFB_FP06_VB` / `COV_LFB_FP06_VB`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-06-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `vb_lfb_create_grnt=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_vb_create_vld` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP06_VB` / `COV_LFB_FP06_VB`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-07-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当完整linefill数据进入refill状态机且D-cache grant有效时；触发信号 `dcache_arb_lfb_ld_grnt`|则四way、全部data word、tag、dirty和ECC来自同一line reference model；交付信号 `lfb_dcache_arb_ld_data_req`|`CHK_LFB_FP07_REFILL` / `COV_LFB_FP07_REFILL`；两data entry与四个refill block使用互异花纹并逐bit对比|
|`LFB-FP-07-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `dcache_arb_lfb_ld_grnt=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_dcache_arb_ld_data_req` 保持二态且只归属于同一owner|`CHK_LFB_FP07_REFILL` / `COV_LFB_FP07_REFILL`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-07-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `dcache_arb_lfb_ld_grnt=1` 与同级竞争条件同拍成立时|则 C1 `lfb_dcache_arb_ld_data_req` 只反映文档定义的winner且不含X|`CHK_LFB_FP07_REFILL` / `COV_LFB_FP07_REFILL`；winner唯一，loser payload零次误消费|
|`LFB-FP-07-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `dcache_arb_lfb_ld_grnt=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_dcache_arb_ld_data_req` 不得产生被flush owner的新副作用|`CHK_LFB_FP07_REFILL` / `COV_LFB_FP07_REFILL`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-07-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `dcache_arb_lfb_ld_grnt=1` 且目标payload取边界值时|则 C1至C3 `lfb_dcache_arb_ld_data_req` 仅在所属accept窗口有效且 `$isunknown(lfb_dcache_arb_ld_data_req)=0`|`CHK_LFB_FP07_REFILL` / `COV_LFB_FP07_REFILL`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-07-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `dcache_arb_lfb_ld_grnt=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_dcache_arb_ld_data_req` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP07_REFILL` / `COV_LFB_FP07_REFILL`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-08-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当CA/NC live地址项以乱序或error response完成时；触发信号 `biu_lsu_r_vld`|则NC empty和all-response状态只在全部适用owner完成后成立；交付信号 `lfb_rb_nc_empty`|`CHK_LFB_FP08_ALL_RESPONSE` / `COV_LFB_FP08_ALL`；混合CA/NC、乱序last、error和长期outstanding逐拍计数|
|`LFB-FP-08-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `biu_lsu_r_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_rb_nc_empty` 保持二态且只归属于同一owner|`CHK_LFB_FP08_ALL_RESPONSE` / `COV_LFB_FP08_ALL`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-08-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `biu_lsu_r_vld=1` 与同级竞争条件同拍成立时|则 C1 `lfb_rb_nc_empty` 只反映文档定义的winner且不含X|`CHK_LFB_FP08_ALL_RESPONSE` / `COV_LFB_FP08_ALL`；winner唯一，loser payload零次误消费|
|`LFB-FP-08-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `biu_lsu_r_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_rb_nc_empty` 不得产生被flush owner的新副作用|`CHK_LFB_FP08_ALL_RESPONSE` / `COV_LFB_FP08_ALL`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-08-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `biu_lsu_r_vld=1` 且目标payload取边界值时|则 C1至C3 `lfb_rb_nc_empty` 仅在所属accept窗口有效且 `$isunknown(lfb_rb_nc_empty)=0`|`CHK_LFB_FP08_ALL_RESPONSE` / `COV_LFB_FP08_ALL`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-08-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `biu_lsu_r_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_rb_nc_empty` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP08_ALL_RESPONSE` / `COV_LFB_FP08_ALL`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-09-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当load miss依赖并入live地址entry且refill、pop或flush发生时；触发信号 `rb_lfb_depd`|则每个有效generation只产生一次wakeup；旧owner bit不得命中复用entry；交付信号 `lfb_mcic_wakeup`|`CHK_LFB_FP09_DEPENDENCY` / `COV_LFB_FP09_WAKEUP`；多依赖merge、部分/full flush和bit立即复用均覆盖|
|`LFB-FP-09-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lfb_depd=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_mcic_wakeup` 保持二态且只归属于同一owner|`CHK_LFB_FP09_DEPENDENCY` / `COV_LFB_FP09_WAKEUP`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-09-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lfb_depd=1` 与同级竞争条件同拍成立时|则 C1 `lfb_mcic_wakeup` 只反映文档定义的winner且不含X|`CHK_LFB_FP09_DEPENDENCY` / `COV_LFB_FP09_WAKEUP`；winner唯一，loser payload零次误消费|
|`LFB-FP-09-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lfb_depd=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_mcic_wakeup` 不得产生被flush owner的新副作用|`CHK_LFB_FP09_DEPENDENCY` / `COV_LFB_FP09_WAKEUP`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-09-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lfb_depd=1` 且目标payload取边界值时|则 C1至C3 `lfb_mcic_wakeup` 仅在所属accept窗口有效且 `$isunknown(lfb_mcic_wakeup)=0`|`CHK_LFB_FP09_DEPENDENCY` / `COV_LFB_FP09_WAKEUP`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-09-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lfb_depd=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_mcic_wakeup` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP09_DEPENDENCY` / `COV_LFB_FP09_WAKEUP`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-10-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当SNQ地址命中已响应的live line时；触发信号 `snq_lfb_vb_req_hit_idx`|则bypass data ID、share和有效期属于同一地址/data owner；交付信号 `lfb_snq_bypass_hit`|`CHK_LFB_FP10_SNQ_BYPASS` / `COV_LFB_FP10_SNQ`；两data项互异花纹、same/different line和多hit负向场景均覆盖|
|`LFB-FP-10-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `snq_lfb_vb_req_hit_idx=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_snq_bypass_hit` 保持二态且只归属于同一owner|`CHK_LFB_FP10_SNQ_BYPASS` / `COV_LFB_FP10_SNQ`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-10-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `snq_lfb_vb_req_hit_idx=1` 与同级竞争条件同拍成立时|则 C1 `lfb_snq_bypass_hit` 只反映文档定义的winner且不含X|`CHK_LFB_FP10_SNQ_BYPASS` / `COV_LFB_FP10_SNQ`；winner唯一，loser payload零次误消费|
|`LFB-FP-10-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `snq_lfb_vb_req_hit_idx=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_snq_bypass_hit` 不得产生被flush owner的新副作用|`CHK_LFB_FP10_SNQ_BYPASS` / `COV_LFB_FP10_SNQ`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-10-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `snq_lfb_vb_req_hit_idx=1` 且目标payload取边界值时|则 C1至C3 `lfb_snq_bypass_hit` 仅在所属accept窗口有效且 `$isunknown(lfb_snq_bypass_hit)=0`|`CHK_LFB_FP10_SNQ_BYPASS` / `COV_LFB_FP10_SNQ`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-10-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `snq_lfb_vb_req_hit_idx=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_snq_bypass_hit` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP10_SNQ_BYPASS` / `COV_LFB_FP10_SNQ`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-11-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当create、VB、R beat、refill或pop阶段遇到full/check flush时；触发信号 `rb_lfb_create_vld`|则依赖队列被清，已发总线事务安全回收且不wakeup被杀owner；交付信号 `lfb_dcache_arb_ld_req`|`CHK_LFB_FP11_FLUSH` / `COV_LFB_FP11_FLUSH`；响应继续与停止两种模型均检查零迟到功能副作用|
|`LFB-FP-11-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lfb_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_dcache_arb_ld_req` 保持二态且只归属于同一owner|`CHK_LFB_FP11_FLUSH` / `COV_LFB_FP11_FLUSH`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-11-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lfb_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lfb_dcache_arb_ld_req` 只反映文档定义的winner且不含X|`CHK_LFB_FP11_FLUSH` / `COV_LFB_FP11_FLUSH`；winner唯一，loser payload零次误消费|
|`LFB-FP-11-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lfb_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_dcache_arb_ld_req` 不得产生被flush owner的新副作用|`CHK_LFB_FP11_FLUSH` / `COV_LFB_FP11_FLUSH`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-11-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lfb_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lfb_dcache_arb_ld_req` 仅在所属accept窗口有效且 `$isunknown(lfb_dcache_arb_ld_req)=0`|`CHK_LFB_FP11_FLUSH` / `COV_LFB_FP11_FLUSH`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-11-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lfb_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_dcache_arb_ld_req` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP11_FLUSH` / `COV_LFB_FP11_FLUSH`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-12-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当地址项和数据项从empty填充至full再drain时；触发信号 `rb_lfb_create_vld`|则full、less2、empty和create反压与reference counter逐拍一致；交付信号 `lfb_addr_full`|`CHK_LFB_FP12_CAPACITY` / `COV_LFB_FP12_CAPACITY`；create/pop同拍、one-left和长期背压不得计数漂移|
|`LFB-FP-12-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lfb_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_addr_full` 保持二态且只归属于同一owner|`CHK_LFB_FP12_CAPACITY` / `COV_LFB_FP12_CAPACITY`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-12-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lfb_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lfb_addr_full` 只反映文档定义的winner且不含X|`CHK_LFB_FP12_CAPACITY` / `COV_LFB_FP12_CAPACITY`；winner唯一，loser payload零次误消费|
|`LFB-FP-12-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lfb_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_addr_full` 不得产生被flush owner的新副作用|`CHK_LFB_FP12_CAPACITY` / `COV_LFB_FP12_CAPACITY`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-12-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lfb_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lfb_addr_full` 仅在所属accept窗口有效且 `$isunknown(lfb_addr_full)=0`|`CHK_LFB_FP12_CAPACITY` / `COV_LFB_FP12_CAPACITY`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-12-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lfb_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_addr_full` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP12_CAPACITY` / `COV_LFB_FP12_CAPACITY`；scoreboard generation递增且旧响应零次命中新owner|
|`LFB-FP-13-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当ICG关闭、scan开启或reset命中状态更新边界时；触发信号 `rb_lfb_create_vld`|则功能事件打开适用gate，reset后empty=1且所有外部请求为0；交付信号 `lfb_empty`|`CHK_LFB_FP13_CLOCK_RESET` / `COV_LFB_FP13_CLOCK`；地址entry、数据entry、linefill状态机和wakeup queue无X或幽灵owner|
|`LFB-FP-13-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lfb_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lfb_empty` 保持二态且只归属于同一owner|`CHK_LFB_FP13_CLOCK_RESET` / `COV_LFB_FP13_CLOCK`；两拍保持期间payload hash和owner均不漂移|
|`LFB-FP-13-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lfb_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `lfb_empty` 只反映文档定义的winner且不含X|`CHK_LFB_FP13_CLOCK_RESET` / `COV_LFB_FP13_CLOCK`；winner唯一，loser payload零次误消费|
|`LFB-FP-13-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lfb_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lfb_dcache_arb_ld_req=0` 且 `lfb_empty` 不得产生被flush owner的新副作用|`CHK_LFB_FP13_CLOCK_RESET` / `COV_LFB_FP13_CLOCK`；flush后的completion/create/forward计数均不增加|
|`LFB-FP-13-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lfb_create_vld=1` 且目标payload取边界值时|则 C1至C3 `lfb_empty` 仅在所属accept窗口有效且 `$isunknown(lfb_empty)=0`|`CHK_LFB_FP13_CLOCK_RESET` / `COV_LFB_FP13_CLOCK`；0/1/N延迟与零/最大值交叉全部命中|
|`LFB-FP-13-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lfb_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lfb_empty` 只对应owner B，旧owner不得迟到修改|`CHK_LFB_FP13_CLOCK_RESET` / `COV_LFB_FP13_CLOCK`；scoreboard generation递增且旧响应零次命中新owner|

<!-- LFB-INTERACTION-2.1-END -->
