# `xx_lsu_ld_da` 功能点与 Test Plan

范围为 DA 数据选择、forward/merge、ECC、异常、RB/LQ/LRQ/LFB 协同和 WB 请求。scoreboard 以 `{lane, IID, generation}` 保存四个 128-bit block、数据源、byte mask 和副作用集合。

| 二级功能点 | 三级功能点 | 功能点描述 | 测试方法和配置说明 | 优先级 |
|---|---|---|---|---|
| 数据选择 | D-cache 16 bank | 根据 way/bank/rot 组合普通 128-bit 与 unit-stride 512-bit 数据；bank 输入见 `srcs/xx_lsu_ld_da.sv:60`～`75` | 16 bank 和四 way 使用互异花纹，穷举 rot/vmew；逐 bit 对比软件拼接模型 | P0 |
| Forward | SQ/WMB/旁路 | SQ、WMB、store-data bypass 与 cache 数据按 byte mask 合并；接口见 `srcs/xx_lsu_ld_da.sv:207`～`227` | none/full/partial forward，冲突源负向激励，检查最新 owner、byte 粒度合并和 PE 标志 | P0 |
| ECC | 检错与 replay | tag/data ECC 可触发 stall、discard、RB 保存，四个 128-bit block 必须完整；ECC 控制见 `srcs/xx_lsu_ld_da.sv:115` | 16 bank × correctable/fatal × 四块互异数据，检查 replay 后 512-bit 无复制/丢块且 completion 一次 | P0 |
| MMU 异常 | 后级 access fault | 请求后一拍 `mmu_lsu_access_fault0` 在 DA 归属当前事务；输入见 `srcs/xx_lsu_ld_da.sv:182` | back-to-back 互异 IID，AF 对每拍单独注入；断言 fault 不串项、错误事务不写数据 | P0 |
| LQ | entry pop | 正常完成、restart/exception 按合同产生 LQ pop，输出见 `srcs/xx_lsu_ld_da.sv:282` | create 后在完成、flush、restart 边界弹出，entry 立即复用；generation scoreboard 阻止旧 pop 杀新 owner | P0 |
| RB | create/merge | cache miss、NC/SO/atomic/ECC 等形成 RB judge/DP/function create 或 merge；接口见 `srcs/xx_lsu_ld_da.sv:308`～`330` | RB 0/1/N 空位，lane 竞争、index hit/merge fail、flush 同拍；检查 winner payload和一次 accept | P0 |
| WB completion | 完成请求 | 正常/异常/no-spec/vector 元数据形成 completion 请求；输出见 `srcs/xx_lsu_ld_da.sv:370`～`383` | completion grant 反压 0/1/N 拍，检查 IID/expt/vstart/halt-info 稳定并恰好一次 | P0 |
| WB data | 标量/向量写回 | DA 发出 data req/DP/gate及四块数据，DP 可宽于功能 req；见 `srcs/xx_lsu_ld_da.sv:372`～`378` | req、DP、gate 合法组合，scalar/FP/vector/US 数据；断言 req implies DP implies gate，DP-only 无写回 valid | P0 |
| 异常/重启 | 优先级与屏蔽 | misalign/PF/AF/ECC/RB full/forward discard 决定 exception、restart、completion；控制集中于 `srcs/xx_lsu_ld_da.sv:4986` | 两两和三项冲突，reference model 检查唯一终态：完成、异常、RB 接收或重启 | P0 |
| LFB/LRQ | 依赖唤醒 | miss/merge/discard 更新 LFB wakeup queue、LRQ bitmap和 late dependency；相关接口见 `srcs/xx_lsu_ld_da.sv:177` | create→miss→LFB response→replay，并在 flush 后复用 LRQ bit；负向旧 wake 必须触发 owner assertion | P1 |
| Debug | halt-info 副作用 | 地址/数据 trigger halt-info 可屏蔽功能和辅助副作用；halt 接口见 `srcs/xx_lsu_ld_da.sv:383` | halt bit 01/10/11，交叉 cache hit/miss/ECC/forward；检查书面合同规定的所有副作用集合 | P1 |
| Flush/Clock | 清除与门控 | full/check flush 在 DA、RB create、WB request 各边界杀死年轻事务；flush 输入见 `srcs/xx_lsu_ld_da.sv:188`～`190` | 每个 side effect 前后注入 flush，ICG on/off/scan；检查旧 owner零次 completion/data/create/pop | P0 |

DA 的 P0 签核必须使用四块互异数据花纹，并分别统计 completion、data、RB create、LQ pop 和异常，避免只靠最终寄存器值掩盖重复副作用。

<!-- DA-INTERACTION-2.1-BEGIN -->

## Interaction 2.1 叶级场景与执行合同

共72行；每个父功能点6行。状态只允许 `BLOCKED_NO_VCS` 或 `PENDING_FULL_CHIP`。
- 四块互异数据用于验证data0/data1/data2/data3不串区。
- completion、RB create、LQ pop、restart必须形成唯一终态。

|场景|setup/周期|当|则|checker/coverage/关闭|
|---|---|---|---|---|
|`DA-FP-01-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ldc_lda_ex2_inst_vld=1` 且四个region选择位有效时|则 `lda_rb_ex3_data_ori` 至data_ori3分别对应四块互异数据且owner一致|`CHK_DA_FP01_DATA_SELECT` / `COV_DA_FP01_DATA`；16 bank至四个128-bit block的选择逐bit匹配|
|`DA-FP-01-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_rb_ex3_data_ori` 保持二态且只归属于同一owner|`CHK_DA_FP01_DATA_SELECT` / `COV_DA_FP01_DATA`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-01-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_rb_ex3_data_ori` 只反映文档定义的winner且不含X|`CHK_DA_FP01_DATA_SELECT` / `COV_DA_FP01_DATA`；winner唯一，loser payload零次误消费|
|`DA-FP-01-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_rb_ex3_data_ori` 不得产生被flush owner的新副作用|`CHK_DA_FP01_DATA_SELECT` / `COV_DA_FP01_DATA`；flush后的completion/create/forward计数均不增加|
|`DA-FP-01-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_rb_ex3_data_ori` 仅在所属accept窗口有效且 `$isunknown(lda_rb_ex3_data_ori)=0`|`CHK_DA_FP01_DATA_SELECT` / `COV_DA_FP01_DATA`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-01-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_rb_ex3_data_ori` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP01_DATA_SELECT` / `COV_DA_FP01_DATA`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-02-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ldc_lda_ex2_fwd_sq_vld=1` 或 `ldc_lda_ex2_fwd_wmb_vld=1` 时|则 `lda_rb_ex3_data_ori` 按byte mask合并选定source且未覆盖byte保持cache数据|`CHK_DA_FP02_FORWARD` / `COV_DA_FP02_FORWARD`；SQ-only、WMB-only、冲突和无forward均使用互异花纹|
|`DA-FP-02-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_rb_ex3_data_ori` 保持二态且只归属于同一owner|`CHK_DA_FP02_FORWARD` / `COV_DA_FP02_FORWARD`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-02-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_rb_ex3_data_ori` 只反映文档定义的winner且不含X|`CHK_DA_FP02_FORWARD` / `COV_DA_FP02_FORWARD`；winner唯一，loser payload零次误消费|
|`DA-FP-02-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_rb_ex3_data_ori` 不得产生被flush owner的新副作用|`CHK_DA_FP02_FORWARD` / `COV_DA_FP02_FORWARD`；flush后的completion/create/forward计数均不增加|
|`DA-FP-02-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_rb_ex3_data_ori` 仅在所属accept窗口有效且 `$isunknown(lda_rb_ex3_data_ori)=0`|`CHK_DA_FP02_FORWARD` / `COV_DA_FP02_FORWARD`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-02-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_rb_ex3_data_ori` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP02_FORWARD` / `COV_DA_FP02_FORWARD`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-03-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `cp0_lsu_ecc_en=1` 且选中bank注入单比特或双比特错误时|则可纠错错误更新数据，fatal错误使 `lda_ex2_ecc_stall=1` 并产生唯一replay/wakeup|`CHK_DA_FP03_ECC` / `COV_DA_FP03_ECC`；无错、单错、双错和连续错误均有明确终态|
|`DA-FP-03-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_ex2_ecc_stall` 保持二态且只归属于同一owner|`CHK_DA_FP03_ECC` / `COV_DA_FP03_ECC`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-03-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_ex2_ecc_stall` 只反映文档定义的winner且不含X|`CHK_DA_FP03_ECC` / `COV_DA_FP03_ECC`；winner唯一，loser payload零次误消费|
|`DA-FP-03-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_ex2_ecc_stall` 不得产生被flush owner的新副作用|`CHK_DA_FP03_ECC` / `COV_DA_FP03_ECC`；flush后的completion/create/forward计数均不增加|
|`DA-FP-03-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_ex2_ecc_stall` 仅在所属accept窗口有效且 `$isunknown(lda_ex2_ecc_stall)=0`|`CHK_DA_FP03_ECC` / `COV_DA_FP03_ECC`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-03-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_ex2_ecc_stall` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP03_ECC` / `COV_DA_FP03_ECC`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-04-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当live EX3 owner对应的 `mmu_lsu_access_fault0=1` 时|则 `lda_lwb_ex3_expt_vld=1` 或RB异常路径有效，且mtval/IID仍属于原owner|`CHK_DA_FP04_ACCESS_FAULT` / `COV_DA_FP04_ACCESS_FAULT`；0/1/N拍fault不串到下一owner|
|`DA-FP-04-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_lwb_ex3_expt_vld` 保持二态且只归属于同一owner|`CHK_DA_FP04_ACCESS_FAULT` / `COV_DA_FP04_ACCESS_FAULT`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-04-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_lwb_ex3_expt_vld` 只反映文档定义的winner且不含X|`CHK_DA_FP04_ACCESS_FAULT` / `COV_DA_FP04_ACCESS_FAULT`；winner唯一，loser payload零次误消费|
|`DA-FP-04-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_lwb_ex3_expt_vld` 不得产生被flush owner的新副作用|`CHK_DA_FP04_ACCESS_FAULT` / `COV_DA_FP04_ACCESS_FAULT`；flush后的completion/create/forward计数均不增加|
|`DA-FP-04-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_lwb_ex3_expt_vld` 仅在所属accept窗口有效且 `$isunknown(lda_lwb_ex3_expt_vld)=0`|`CHK_DA_FP04_ACCESS_FAULT` / `COV_DA_FP04_ACCESS_FAULT`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-04-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_lwb_ex3_expt_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP04_ACCESS_FAULT` / `COV_DA_FP04_ACCESS_FAULT`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-05-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ldc_lda_ex2_inst_vld=1` 到达唯一终态且无需restart时|则 `lda_ex3_lq_entry_pop` 只包含原LQ entry且 `lda_idu_ex3_pop_vld=1`|`CHK_DA_FP05_LQ_POP` / `COV_DA_FP05_LQ_POP`；LQ pop每个owner恰好一次，flush/restart为零次|
|`DA-FP-05-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_ex3_lq_entry_pop` 保持二态且只归属于同一owner|`CHK_DA_FP05_LQ_POP` / `COV_DA_FP05_LQ_POP`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-05-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_ex3_lq_entry_pop` 只反映文档定义的winner且不含X|`CHK_DA_FP05_LQ_POP` / `COV_DA_FP05_LQ_POP`；winner唯一，loser payload零次误消费|
|`DA-FP-05-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_ex3_lq_entry_pop` 不得产生被flush owner的新副作用|`CHK_DA_FP05_LQ_POP` / `COV_DA_FP05_LQ_POP`；flush后的completion/create/forward计数均不增加|
|`DA-FP-05-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_ex3_lq_entry_pop` 仅在所属accept窗口有效且 `$isunknown(lda_ex3_lq_entry_pop)=0`|`CHK_DA_FP05_LQ_POP` / `COV_DA_FP05_LQ_POP`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-05-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_ex3_lq_entry_pop` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP05_LQ_POP` / `COV_DA_FP05_LQ_POP`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-06-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ldc_lda_ex2_inst_vld=1` 且RB非full时|则miss产生唯一 `lda_rb_ex3_create_vld=1`，命中产生 `lda_rb_ex3_merge_vld=1`，两者互斥|`CHK_DA_FP06_RB_OWNER` / `COV_DA_FP06_RB`；RB create/merge winner与payload owner一致|
|`DA-FP-06-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_rb_ex3_create_vld` 保持二态且只归属于同一owner|`CHK_DA_FP06_RB_OWNER` / `COV_DA_FP06_RB`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-06-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_rb_ex3_create_vld` 只反映文档定义的winner且不含X|`CHK_DA_FP06_RB_OWNER` / `COV_DA_FP06_RB`；winner唯一，loser payload零次误消费|
|`DA-FP-06-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_rb_ex3_create_vld` 不得产生被flush owner的新副作用|`CHK_DA_FP06_RB_OWNER` / `COV_DA_FP06_RB`；flush后的completion/create/forward计数均不增加|
|`DA-FP-06-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_rb_ex3_create_vld` 仅在所属accept窗口有效且 `$isunknown(lda_rb_ex3_create_vld)=0`|`CHK_DA_FP06_RB_OWNER` / `COV_DA_FP06_RB`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-06-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_rb_ex3_create_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP06_RB_OWNER` / `COV_DA_FP06_RB`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-07-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ldc_lda_ex2_inst_vld=1` 的load在DA到达completion终态时|则 `lda_lwb_ex3_cmplt_req=1` 必须伴随 `lda_lwb_ex3_cmplt_req_gate=1` 且只脉冲一次|`CHK_DA_FP07_COMPLETION` / `COV_DA_FP07_COMPLETION`；completion与RB create、restart终态互斥|
|`DA-FP-07-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_lwb_ex3_cmplt_req` 保持二态且只归属于同一owner|`CHK_DA_FP07_COMPLETION` / `COV_DA_FP07_COMPLETION`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-07-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_lwb_ex3_cmplt_req` 只反映文档定义的winner且不含X|`CHK_DA_FP07_COMPLETION` / `COV_DA_FP07_COMPLETION`；winner唯一，loser payload零次误消费|
|`DA-FP-07-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_lwb_ex3_cmplt_req` 不得产生被flush owner的新副作用|`CHK_DA_FP07_COMPLETION` / `COV_DA_FP07_COMPLETION`；flush后的completion/create/forward计数均不增加|
|`DA-FP-07-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_lwb_ex3_cmplt_req` 仅在所属accept窗口有效且 `$isunknown(lda_lwb_ex3_cmplt_req)=0`|`CHK_DA_FP07_COMPLETION` / `COV_DA_FP07_COMPLETION`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-07-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_lwb_ex3_cmplt_req` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP07_COMPLETION` / `COV_DA_FP07_COMPLETION`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-08-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当无异常且 `ldc_lda_ex2_inst_vld=1` 的owner命中数据时|则 `lda_lwb_ex3_data_req=1` 蕴含DP/gate有效，scalar只用data0，US检查四块互异数据|`CHK_DA_FP08_DATA_REQ` / `COV_DA_FP08_DATA`；数据请求、mask和owner元数据同拍一致|
|`DA-FP-08-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_lwb_ex3_data_req` 保持二态且只归属于同一owner|`CHK_DA_FP08_DATA_REQ` / `COV_DA_FP08_DATA`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-08-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_lwb_ex3_data_req` 只反映文档定义的winner且不含X|`CHK_DA_FP08_DATA_REQ` / `COV_DA_FP08_DATA`；winner唯一，loser payload零次误消费|
|`DA-FP-08-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_lwb_ex3_data_req` 不得产生被flush owner的新副作用|`CHK_DA_FP08_DATA_REQ` / `COV_DA_FP08_DATA`；flush后的completion/create/forward计数均不增加|
|`DA-FP-08-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_lwb_ex3_data_req` 仅在所属accept窗口有效且 `$isunknown(lda_lwb_ex3_data_req)=0`|`CHK_DA_FP08_DATA_REQ` / `COV_DA_FP08_DATA`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-08-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_lwb_ex3_data_req` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP08_DATA_REQ` / `COV_DA_FP08_DATA`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-09-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ldc_lda_ex2_inst_vld=1` 且异常、spec-fail或RB full条件命中时|则completion、RB create、LQ pop、restart中恰有一个唯一终态，`lda_lwb_ex3_expt_vld` 仅随异常owner|`CHK_DA_FP09_TERMINAL` / `COV_DA_FP09_TERMINAL`；所有两两交叉无双重副作用|
|`DA-FP-09-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_lwb_ex3_expt_vld` 保持二态且只归属于同一owner|`CHK_DA_FP09_TERMINAL` / `COV_DA_FP09_TERMINAL`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-09-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_lwb_ex3_expt_vld` 只反映文档定义的winner且不含X|`CHK_DA_FP09_TERMINAL` / `COV_DA_FP09_TERMINAL`；winner唯一，loser payload零次误消费|
|`DA-FP-09-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_lwb_ex3_expt_vld` 不得产生被flush owner的新副作用|`CHK_DA_FP09_TERMINAL` / `COV_DA_FP09_TERMINAL`；flush后的completion/create/forward计数均不增加|
|`DA-FP-09-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_lwb_ex3_expt_vld` 仅在所属accept窗口有效且 `$isunknown(lda_lwb_ex3_expt_vld)=0`|`CHK_DA_FP09_TERMINAL` / `COV_DA_FP09_TERMINAL`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-09-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_lwb_ex3_expt_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP09_TERMINAL` / `COV_DA_FP09_TERMINAL`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-10-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `ldc_lda_ex2_inst_vld=1` 的owner产生LFB依赖或DA反馈时|则 `lda_lfb_set_wakeup_queue` 和LRQ bitmap只包含保存LSID且每个事件一次|`CHK_DA_FP10_WAKEUP` / `COV_DA_FP10_WAKEUP`；flush后零次迟到wakeup，entry复用不串owner|
|`DA-FP-10-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_lfb_set_wakeup_queue` 保持二态且只归属于同一owner|`CHK_DA_FP10_WAKEUP` / `COV_DA_FP10_WAKEUP`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-10-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_lfb_set_wakeup_queue` 只反映文档定义的winner且不含X|`CHK_DA_FP10_WAKEUP` / `COV_DA_FP10_WAKEUP`；winner唯一，loser payload零次误消费|
|`DA-FP-10-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_lfb_set_wakeup_queue` 不得产生被flush owner的新副作用|`CHK_DA_FP10_WAKEUP` / `COV_DA_FP10_WAKEUP`；flush后的completion/create/forward计数均不增加|
|`DA-FP-10-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_lfb_set_wakeup_queue` 仅在所属accept窗口有效且 `$isunknown(lda_lfb_set_wakeup_queue)=0`|`CHK_DA_FP10_WAKEUP` / `COV_DA_FP10_WAKEUP`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-10-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_lfb_set_wakeup_queue` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP10_WAKEUP` / `COV_DA_FP10_WAKEUP`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-11-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `dtu_lsu_data_trig_en=1` 命中live owner时|则 `ld_da_idu_halt_info_update_vld` 仅更新该owner，cancel/flush后不产生迟到副作用|`CHK_DA_FP11_DEBUG` / `COV_DA_FP11_DEBUG`；halt-info、IID和触发类型三者一致|
|`DA-FP-11-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `ld_da_idu_halt_info_update_vld` 保持二态且只归属于同一owner|`CHK_DA_FP11_DEBUG` / `COV_DA_FP11_DEBUG`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-11-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `ld_da_idu_halt_info_update_vld` 只反映文档定义的winner且不含X|`CHK_DA_FP11_DEBUG` / `COV_DA_FP11_DEBUG`；winner唯一，loser payload零次误消费|
|`DA-FP-11-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `ld_da_idu_halt_info_update_vld` 不得产生被flush owner的新副作用|`CHK_DA_FP11_DEBUG` / `COV_DA_FP11_DEBUG`；flush后的completion/create/forward计数均不增加|
|`DA-FP-11-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `ld_da_idu_halt_info_update_vld` 仅在所属accept窗口有效且 `$isunknown(ld_da_idu_halt_info_update_vld)=0`|`CHK_DA_FP11_DEBUG` / `COV_DA_FP11_DEBUG`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-11-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `ld_da_idu_halt_info_update_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP11_DEBUG` / `COV_DA_FP11_DEBUG`；scoreboard generation递增且旧响应零次命中新owner|
|`DA-FP-12-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `rtu_lsu_flush_fe=1` 或reset命中live DA owner时|则下一拍 `lda_ex3_inst_vld=0`，ICG/scan只改变clock可达性而不改变功能owner|`CHK_DA_FP12_FLUSH_CLOCK` / `COV_DA_FP12_CLOCK`；flush后completion/data/RB/LQ副作用均为零|
|`DA-FP-12-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `ldc_lda_ex2_inst_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `lda_ex3_inst_vld` 保持二态且只归属于同一owner|`CHK_DA_FP12_FLUSH_CLOCK` / `COV_DA_FP12_CLOCK`；两拍保持期间payload hash和owner均不漂移|
|`DA-FP-12-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `ldc_lda_ex2_inst_vld=1` 与同级竞争条件同拍成立时|则 C1 `lda_ex3_inst_vld` 只反映文档定义的winner且不含X|`CHK_DA_FP12_FLUSH_CLOCK` / `COV_DA_FP12_CLOCK`；winner唯一，loser payload零次误消费|
|`DA-FP-12-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `ldc_lda_ex2_inst_vld=1` 与 `rtu_lsu_flush_fe=1` 在owner窗口交叉时|则 C2 `lda_ex3_inst_vld=0` 且 `lda_ex3_inst_vld` 不得产生被flush owner的新副作用|`CHK_DA_FP12_FLUSH_CLOCK` / `COV_DA_FP12_CLOCK`；flush后的completion/create/forward计数均不增加|
|`DA-FP-12-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `ldc_lda_ex2_inst_vld=1` 且目标payload取边界值时|则 C1至C3 `lda_ex3_inst_vld` 仅在所属accept窗口有效且 `$isunknown(lda_ex3_inst_vld)=0`|`CHK_DA_FP12_FLUSH_CLOCK` / `COV_DA_FP12_CLOCK`；0/1/N延迟与零/最大值交叉全部命中|
|`DA-FP-12-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `ldc_lda_ex2_inst_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `lda_ex3_inst_vld` 只对应owner B，旧owner不得迟到修改|`CHK_DA_FP12_FLUSH_CLOCK` / `COV_DA_FP12_CLOCK`；scoreboard generation递增且旧响应零次命中新owner|

<!-- DA-INTERACTION-2.1-END -->
