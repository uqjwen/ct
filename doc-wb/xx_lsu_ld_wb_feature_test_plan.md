# `xx_lsu_ld_wb` 功能点与 Test Plan

范围为 completion/data 双通道仲裁、标量/浮点/向量格式化、RTU/IDU 输出、bus error、debug halt-info 和门控。每个 requester 必须满足 hold-until-grant。

| 二级功能点 | 三级功能点 | 功能点描述 | 测试方法和配置说明 | 优先级 |
|---|---|---|---|---|
| Completion 仲裁 | DA/RB 竞争 | DA 与 RB completion 请求选择唯一 winner并返回 grant；输入见 `srcs/xx_lsu_ld_wb.sv:61`、`87` | 00/01/10/11 及连续争用，loser 随机保持 N 拍；检查 grant、IID、expt、vstart 不串项 | P0 |
| Data 仲裁 | DA/WMB/VMB/RB | 四类 data request 竞争共享写回资源；输入分布于 `srcs/xx_lsu_ld_wb.sv:68`～`139` | 穷举 16 种组合并持续高优先级流量，检查 onehot grant、payload winner和有界公平合同 | P0 |
| 请求合同 | req/DP/gate | DA data req 必须蕴含 DP，DP 必须蕴含 gate；接口见 `srcs/xx_lsu_ld_wb.sv:68`～`70` | 合法 DP-only 和负向非法组合；断言 req implies DP、DP implies gate，DP-only 不产生写回 | P0 |
| 标量写回 | PREG 与符号扩展 | 按 size/sign 控制生成 64-bit integer/FP 数据和 preg valid；输出见 `srcs/xx_lsu_ld_wb.sv:159`～`162` | byte/half/word/dword、signed/unsigned、边界值，reference model 逐 bit 检查 | P0 |
| 向量写回 | VR0/VR1/FR | 根据 vector 类型、vmew、sign select 和 4×128-bit payload形成各端口；见 `srcs/xx_lsu_ld_wb.sv:163`～`170` | vector/FP/US、互异四块数据和所有合法 sign select；检查 lane、expand 与 valid 一致 | P0 |
| RTU 完成 | completion/exception | 输出 load pipe completion、exception、flush、preg/vreg有效信息；接口见 `srcs/xx_lsu_ld_wb.sv:177`～`195` | normal/PF/AF/bus error/no-spec/FOF，交叉 completion 与 data 分拍；RTU scoreboard 检查一次终态 | P0 |
| Bus error | 数据抑制 | RB/LFB 返回错误时生成异常并禁止错误数据写入寄存器；RB 输入区见 `srcs/xx_lsu_ld_wb.sv:87`～`116` | R/B response error、boundary 两半、US 两 beat 各位置注错；检查 mtval、exception和 data valid | P0 |
| VMB completion | merge/FOF | VMB completion带 element count、exception、vsetvl、VMB ID和数据；输出见 `srcs/xx_lsu_ld_wb.sv:145`～`150` | merge/non-merge、FOF first/non-first、vsetvl组合；检查 VMB ID、VL/vstart和完成次数 | P1 |
| Debug | halt-info 更新 | DA/RB data trigger 经两级 valid 更新 completion effect；接口见 `srcs/xx_lsu_ld_wb.sv:200`～`215` | halt bit 01/10/11，各打一拍后停止所有流量；检查 update/effect 可自行开钟并有界清零 | P0 |
| Forward | EX4 旁路 | 写回结果同步提供 IDU EX4 preg/vreg forward；输出见 `srcs/xx_lsu_ld_wb.sv:157`～`170` | RAW consumer 紧随 producer，交叉 scalar/vector、grant 反压和 exception；检查旁路仅来自实际 winner | P1 |
| Flush | 年轻请求取消 | full/check flush 禁止被杀请求继续 completion/data；输入见 `srcs/xx_lsu_ld_wb.sv:119`～`121` | 在请求、grant、输出各拍注入 flush，包含 IID wrap；检查旧 owner不写回且合法老请求保留 | P0 |
| Clock/Reset | completion/data ICG | 两类时钟必须覆盖请求、grant和 halt-info 的置位/清除边沿；相关控制由 `srcs/xx_lsu_ld_wb.sv:31` 模块内生成 | ICG on/off/scan、空闲后一拍 trigger、reset 释放；比较组合 enable 与实际 clock pulse和寄存结果 | P0 |

动态签核需同时连接 `xx_lsu_wb_arbiter`，并对每个被反压 requester 检查 payload 稳定；只验证 winner 数据不足以证明请求不会丢失。

<!-- WB-INTERACTION-2.1-BEGIN -->

## Interaction 2.1 叶级场景与执行合同

共72行；每个父功能点6行。状态只允许 `BLOCKED_NO_VCS` 或 `PENDING_FULL_CHIP`。
- 请求蕴含链：req=1 时必须 DP=1 且 gate=1；DP-only只允许预开数据路径。
- 持续请求可在任意空闲lane获得服务，完成和数据不会丢失。

|场景|setup/周期|当|则|checker/coverage/关闭|
|---|---|---|---|---|
|`WB-FP-01-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lda_lwb_ex3_cmplt_req=1` 与 `rb_lwb_ex3_cmplt_req=1` 同拍竞争时|则completion winner唯一，`lwb_rb_ex3_cmplt_grnt` 仅在RB获胜时为1且loser保持|`CHK_WB_FP01_CMPLT_ARB` / `COV_WB_FP01_CMPLT`；固定优先级和持续请求均不会丢失completion|
|`WB-FP-01-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_cmplt_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lwb_rb_ex3_cmplt_grnt` 保持二态且只归属于同一owner|`CHK_WB_FP01_CMPLT_ARB` / `COV_WB_FP01_CMPLT`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-01-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_cmplt_req=1` 与同级竞争条件同拍成立时|则 C1 `lwb_rb_ex3_cmplt_grnt` 只反映文档定义的winner且不含X|`CHK_WB_FP01_CMPLT_ARB` / `COV_WB_FP01_CMPLT`；winner唯一，loser payload零次误消费|
|`WB-FP-01-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_cmplt_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lwb_rb_ex3_cmplt_grnt` 不得产生被flush owner的新副作用|`CHK_WB_FP01_CMPLT_ARB` / `COV_WB_FP01_CMPLT`；flush后的completion/create/forward计数均不增加|
|`WB-FP-01-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_cmplt_req=1` 且目标payload取边界值时|则 C1至C3 `lwb_rb_ex3_cmplt_grnt` 仅在所属accept窗口有效且 `$isunknown(lwb_rb_ex3_cmplt_grnt)=0`|`CHK_WB_FP01_CMPLT_ARB` / `COV_WB_FP01_CMPLT`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-01-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_cmplt_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lwb_rb_ex3_cmplt_grnt` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP01_CMPLT_ARB` / `COV_WB_FP01_CMPLT`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-02-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lda_lwb_ex3_data_req`、WMB、VMB、RB请求任意组合且至少一个为1时|则 `lwb_ex4_data_vld=1` 且三个显式grant至多一个，winner payload唯一|`CHK_WB_FP02_DATA_ARB` / `COV_WB_FP02_DATA`；四源互异数据、持续竞争和轮换空闲lane不会丢失|
|`WB-FP-02-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lwb_ex4_data_vld` 保持二态且只归属于同一owner|`CHK_WB_FP02_DATA_ARB` / `COV_WB_FP02_DATA`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-02-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `lwb_ex4_data_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP02_DATA_ARB` / `COV_WB_FP02_DATA`；winner唯一，loser payload零次误消费|
|`WB-FP-02-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lwb_ex4_data_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP02_DATA_ARB` / `COV_WB_FP02_DATA`；flush后的completion/create/forward计数均不增加|
|`WB-FP-02-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `lwb_ex4_data_vld` 仅在所属accept窗口有效且 `$isunknown(lwb_ex4_data_vld)=0`|`CHK_WB_FP02_DATA_ARB` / `COV_WB_FP02_DATA`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-02-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lwb_ex4_data_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP02_DATA_ARB` / `COV_WB_FP02_DATA`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-03-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lda_lwb_ex3_data_req=1` 时必须同时满足DP和gate合同|则req=1蕴含DP=1和gate=1；DP-only只可预开数据路径且 `lwb_ex4_data_vld=0`|`CHK_WB_FP03_REQ_DP_GATE` / `COV_WB_FP03_REQ`；req-only、DP-only、gate-only及全部有效四种组合均覆盖|
|`WB-FP-03-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lwb_ex4_data_vld` 保持二态且只归属于同一owner|`CHK_WB_FP03_REQ_DP_GATE` / `COV_WB_FP03_REQ`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-03-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `lwb_ex4_data_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP03_REQ_DP_GATE` / `COV_WB_FP03_REQ`；winner唯一，loser payload零次误消费|
|`WB-FP-03-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lwb_ex4_data_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP03_REQ_DP_GATE` / `COV_WB_FP03_REQ`；flush后的completion/create/forward计数均不增加|
|`WB-FP-03-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `lwb_ex4_data_vld` 仅在所属accept窗口有效且 `$isunknown(lwb_ex4_data_vld)=0`|`CHK_WB_FP03_REQ_DP_GATE` / `COV_WB_FP03_REQ`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-03-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lwb_ex4_data_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP03_REQ_DP_GATE` / `COV_WB_FP03_REQ`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-04-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当scalar `lda_lwb_ex3_data_req=1` 被仲裁接受时|则 `lsu_idu_ex4_preg_vld=1` 且PREG、符号扩展数据属于同一DA owner|`CHK_WB_FP04_SCALAR` / `COV_WB_FP04_SCALAR`；byte/half/word/dword和正负符号边界逐bit检查|
|`WB-FP-04-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_idu_ex4_preg_vld` 保持二态且只归属于同一owner|`CHK_WB_FP04_SCALAR` / `COV_WB_FP04_SCALAR`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-04-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `lsu_idu_ex4_preg_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP04_SCALAR` / `COV_WB_FP04_SCALAR`；winner唯一，loser payload零次误消费|
|`WB-FP-04-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lsu_idu_ex4_preg_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP04_SCALAR` / `COV_WB_FP04_SCALAR`；flush后的completion/create/forward计数均不增加|
|`WB-FP-04-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `lsu_idu_ex4_preg_vld` 仅在所属accept窗口有效且 `$isunknown(lsu_idu_ex4_preg_vld)=0`|`CHK_WB_FP04_SCALAR` / `COV_WB_FP04_SCALAR`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-04-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_idu_ex4_preg_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP04_SCALAR` / `COV_WB_FP04_SCALAR`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-05-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当vector或FP `lda_lwb_ex3_data_req=1` 被接受时|则VR0、VR1、FR中只有适用通道valid且 `lsu_idu_ex4_vreg_vld=1`|`CHK_WB_FP05_VECTOR` / `COV_WB_FP05_VECTOR`；vmew、split、FR/VR选择和互异数据花纹全部覆盖|
|`WB-FP-05-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_idu_ex4_vreg_vld` 保持二态且只归属于同一owner|`CHK_WB_FP05_VECTOR` / `COV_WB_FP05_VECTOR`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-05-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `lsu_idu_ex4_vreg_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP05_VECTOR` / `COV_WB_FP05_VECTOR`；winner唯一，loser payload零次误消费|
|`WB-FP-05-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lsu_idu_ex4_vreg_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP05_VECTOR` / `COV_WB_FP05_VECTOR`；flush后的completion/create/forward计数均不增加|
|`WB-FP-05-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `lsu_idu_ex4_vreg_vld` 仅在所属accept窗口有效且 `$isunknown(lsu_idu_ex4_vreg_vld)=0`|`CHK_WB_FP05_VECTOR` / `COV_WB_FP05_VECTOR`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-05-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_idu_ex4_vreg_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP05_VECTOR` / `COV_WB_FP05_VECTOR`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-06-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lda_lwb_ex3_cmplt_req=1` 携带IID和异常元数据时|则 `lsu_rtu_ex4_cmplt=1`、IID精确匹配且exception只对异常owner有效|`CHK_WB_FP06_RTU` / `COV_WB_FP06_RTU`；completion每个owner一次且异常/正常元数据互斥|
|`WB-FP-06-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_cmplt_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_rtu_ex4_cmplt` 保持二态且只归属于同一owner|`CHK_WB_FP06_RTU` / `COV_WB_FP06_RTU`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-06-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_cmplt_req=1` 与同级竞争条件同拍成立时|则 C1 `lsu_rtu_ex4_cmplt` 只反映文档定义的winner且不含X|`CHK_WB_FP06_RTU` / `COV_WB_FP06_RTU`；winner唯一，loser payload零次误消费|
|`WB-FP-06-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_cmplt_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lsu_rtu_ex4_cmplt` 不得产生被flush owner的新副作用|`CHK_WB_FP06_RTU` / `COV_WB_FP06_RTU`；flush后的completion/create/forward计数均不增加|
|`WB-FP-06-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_cmplt_req=1` 且目标payload取边界值时|则 C1至C3 `lsu_rtu_ex4_cmplt` 仅在所属accept窗口有效且 `$isunknown(lsu_rtu_ex4_cmplt)=0`|`CHK_WB_FP06_RTU` / `COV_WB_FP06_RTU`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-06-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_cmplt_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_rtu_ex4_cmplt` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP06_RTU` / `COV_WB_FP06_RTU`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-07-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `rb_lwb_ex3_bus_err=1` 随RB data request到达时|则 `lsu_rtu_async_expt_vld=1` 且地址匹配，同时正常 `lsu_idu_ex4_preg_vld=0`|`CHK_WB_FP07_BUS_ERROR` / `COV_WB_FP07_BUS_ERROR`；错误数据零次进入架构寄存器|
|`WB-FP-07-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_rtu_async_expt_vld` 保持二态且只归属于同一owner|`CHK_WB_FP07_BUS_ERROR` / `COV_WB_FP07_BUS_ERROR`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-07-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `lsu_rtu_async_expt_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP07_BUS_ERROR` / `COV_WB_FP07_BUS_ERROR`；winner唯一，loser payload零次误消费|
|`WB-FP-07-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lsu_rtu_async_expt_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP07_BUS_ERROR` / `COV_WB_FP07_BUS_ERROR`；flush后的completion/create/forward计数均不增加|
|`WB-FP-07-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `lsu_rtu_async_expt_vld` 仅在所属accept窗口有效且 `$isunknown(lsu_rtu_async_expt_vld)=0`|`CHK_WB_FP07_BUS_ERROR` / `COV_WB_FP07_BUS_ERROR`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-07-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_rtu_async_expt_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP07_BUS_ERROR` / `COV_WB_FP07_BUS_ERROR`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-08-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `vmb_lwb_data_req=1` 或VMB completion被接受时|则 `ld_wb_vmb_data_grnt`、merge、VMB ID和FOF/completion元数据来自同一owner|`CHK_WB_FP08_VMB` / `COV_WB_FP08_VMB`；merge/non-merge与data/completion交叉无ID串项|
|`WB-FP-08-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `vmb_lwb_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `ld_wb_vmb_data_grnt` 保持二态且只归属于同一owner|`CHK_WB_FP08_VMB` / `COV_WB_FP08_VMB`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-08-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `vmb_lwb_data_req=1` 与同级竞争条件同拍成立时|则 C1 `ld_wb_vmb_data_grnt` 只反映文档定义的winner且不含X|`CHK_WB_FP08_VMB` / `COV_WB_FP08_VMB`；winner唯一，loser payload零次误消费|
|`WB-FP-08-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `vmb_lwb_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `ld_wb_vmb_data_grnt` 不得产生被flush owner的新副作用|`CHK_WB_FP08_VMB` / `COV_WB_FP08_VMB`；flush后的completion/create/forward计数均不增加|
|`WB-FP-08-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `vmb_lwb_data_req=1` 且目标payload取边界值时|则 C1至C3 `ld_wb_vmb_data_grnt` 仅在所属accept窗口有效且 `$isunknown(ld_wb_vmb_data_grnt)=0`|`CHK_WB_FP08_VMB` / `COV_WB_FP08_VMB`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-08-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `vmb_lwb_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `ld_wb_vmb_data_grnt` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP08_VMB` / `COV_WB_FP08_VMB`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-09-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `rb_ld_wb_data_check=1` 随RB owner进入WB时|则 `rb_entry_data_halt_info_update_vld` 只命中该entry且debug data valid为单拍|`CHK_WB_FP09_DEBUG` / `COV_WB_FP09_DEBUG`；更新后自清，下一owner不会继承旧halt-info|
|`WB-FP-09-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `rb_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_entry_data_halt_info_update_vld` 保持二态且只归属于同一owner|`CHK_WB_FP09_DEBUG` / `COV_WB_FP09_DEBUG`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-09-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `rb_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `rb_entry_data_halt_info_update_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP09_DEBUG` / `COV_WB_FP09_DEBUG`；winner唯一，loser payload零次误消费|
|`WB-FP-09-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `rb_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `rb_entry_data_halt_info_update_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP09_DEBUG` / `COV_WB_FP09_DEBUG`；flush后的completion/create/forward计数均不增加|
|`WB-FP-09-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `rb_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `rb_entry_data_halt_info_update_vld` 仅在所属accept窗口有效且 `$isunknown(rb_entry_data_halt_info_update_vld)=0`|`CHK_WB_FP09_DEBUG` / `COV_WB_FP09_DEBUG`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-09-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `rb_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_entry_data_halt_info_update_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP09_DEBUG` / `COV_WB_FP09_DEBUG`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-10-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lda_lwb_ex3_data_req=1` 或其他data source赢得WB仲裁且目标为vector时|则 `lsu_idu_ex4_fwd_vreg_vld=1` 且forward寄存器只来自实际winner|`CHK_WB_FP10_FORWARD` / `COV_WB_FP10_FORWARD`；loser payload变化不得影响EX4 bypass|
|`WB-FP-10-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lsu_idu_ex4_fwd_vreg_vld` 保持二态且只归属于同一owner|`CHK_WB_FP10_FORWARD` / `COV_WB_FP10_FORWARD`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-10-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `lsu_idu_ex4_fwd_vreg_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP10_FORWARD` / `COV_WB_FP10_FORWARD`；winner唯一，loser payload零次误消费|
|`WB-FP-10-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lsu_idu_ex4_fwd_vreg_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP10_FORWARD` / `COV_WB_FP10_FORWARD`；flush后的completion/create/forward计数均不增加|
|`WB-FP-10-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `lsu_idu_ex4_fwd_vreg_vld` 仅在所属accept窗口有效且 `$isunknown(lsu_idu_ex4_fwd_vreg_vld)=0`|`CHK_WB_FP10_FORWARD` / `COV_WB_FP10_FORWARD`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-10-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lsu_idu_ex4_fwd_vreg_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP10_FORWARD` / `COV_WB_FP10_FORWARD`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-11-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `rtu_ck_flush=1` 且flush IID年龄命中当前owner时|则 `lwb_ex4_inst_vld=0`、completion/data valid均不产生；未命中owner保留|`CHK_WB_FP11_FLUSH` / `COV_WB_FP11_FLUSH`；IID wrap和full/check flush边界均覆盖|
|`WB-FP-11-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_cmplt_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lwb_ex4_inst_vld` 保持二态且只归属于同一owner|`CHK_WB_FP11_FLUSH` / `COV_WB_FP11_FLUSH`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-11-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_cmplt_req=1` 与同级竞争条件同拍成立时|则 C1 `lwb_ex4_inst_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP11_FLUSH` / `COV_WB_FP11_FLUSH`；winner唯一，loser payload零次误消费|
|`WB-FP-11-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_cmplt_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lwb_ex4_inst_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP11_FLUSH` / `COV_WB_FP11_FLUSH`；flush后的completion/create/forward计数均不增加|
|`WB-FP-11-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_cmplt_req=1` 且目标payload取边界值时|则 C1至C3 `lwb_ex4_inst_vld` 仅在所属accept窗口有效且 `$isunknown(lwb_ex4_inst_vld)=0`|`CHK_WB_FP11_FLUSH` / `COV_WB_FP11_FLUSH`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-11-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_cmplt_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lwb_ex4_inst_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP11_FLUSH` / `COV_WB_FP11_FLUSH`；scoreboard generation递增且旧响应零次命中新owner|
|`WB-FP-12-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `cp0_lsu_icg_en=0`、scan=1且合法请求到达时|则scan允许捕获，reset低时 `lwb_ex4_data_vld=0` 且所有架构valid为0|`CHK_WB_FP12_CLOCK_RESET` / `COV_WB_FP12_CLOCK`；ICG on/off/scan和reset释放首拍无幽灵写回|
|`WB-FP-12-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda_lwb_ex3_data_req=1` 且同一owner连续保持两拍时|则 C1至C2 `lwb_ex4_data_vld` 保持二态且只归属于同一owner|`CHK_WB_FP12_CLOCK_RESET` / `COV_WB_FP12_CLOCK`；两拍保持期间payload hash和owner均不漂移|
|`WB-FP-12-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda_lwb_ex3_data_req=1` 与同级竞争条件同拍成立时|则 C1 `lwb_ex4_data_vld` 只反映文档定义的winner且不含X|`CHK_WB_FP12_CLOCK_RESET` / `COV_WB_FP12_CLOCK`；winner唯一，loser payload零次误消费|
|`WB-FP-12-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda_lwb_ex3_data_req=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `lwb_ex4_inst_vld=0` 且 `lwb_ex4_data_vld` 不得产生被flush owner的新副作用|`CHK_WB_FP12_CLOCK_RESET` / `COV_WB_FP12_CLOCK`；flush后的completion/create/forward计数均不增加|
|`WB-FP-12-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda_lwb_ex3_data_req=1` 且目标payload取边界值时|则 C1至C3 `lwb_ex4_data_vld` 仅在所属accept窗口有效且 `$isunknown(lwb_ex4_data_vld)=0`|`CHK_WB_FP12_CLOCK_RESET` / `COV_WB_FP12_CLOCK`；0/1/N延迟与零/最大值交叉全部命中|
|`WB-FP-12-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda_lwb_ex3_data_req=1` 的资源在下一合法拍被新owner复用时|则 C3 `lwb_ex4_data_vld` 只对应owner B，旧owner不得迟到修改|`CHK_WB_FP12_CLOCK_RESET` / `COV_WB_FP12_CLOCK`；scoreboard generation递增且旧响应零次命中新owner|

<!-- WB-INTERACTION-2.1-END -->
