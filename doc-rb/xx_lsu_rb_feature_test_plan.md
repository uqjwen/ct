# `xx_lsu_rb` / `xx_lsu_rb_entry` 功能点与 Test Plan

范围包含 RB allocation/merge/仲裁，以及 `xx_lsu_rb_entry` 从 create、BIU request/response、LFB、completion/data writeback 到释放的完整生命周期。scoreboard 键为 `{entry, IID, generation, BIU ID, owner}`。

| 二级功能点 | 三级功能点 | 功能点描述 | 测试方法和配置说明 | 优先级 |
|---|---|---|---|---|
| 容量管理 | pointer/保留项 | 普通、MMU和多 lane请求依据空闲项生成不重叠 create pointer及 full反馈；模块入口见 `srcs/xx_lsu_rb.sv:31` | 0/1/2/3 空位和保留项，三 lane同拍 create；断言 success对应唯一 pointer且普通请求不占保留项 | P0 |
| Create | winner payload | judge、DP、function valid逐级收紧，低优先级成功项必须采样自己的 payload；创建输入见 `srcs/xx_lsu_rb.sv:222`～`251` | 高优先级 DP 有效但功能 create失败、低优先级成功；entry内使用互异 IID/data 检查 owner | P0 |
| Entry 状态机 | create→request→response | `xx_lsu_rb_entry` 保存事务并推进 BIU、wait response、WB和pop状态；模块见 `srcs/xx_lsu_rb_entry.sv:25` | 每条状态边随机反压0/1/N拍，断言合法转移、无跳态和每事务只释放一次 | P0 |
| Merge | cache line/boundary | 同 line请求可 merge或保存 boundary second part，byte mask/data owner不丢；merge输出见 `srcs/xx_lsu_rb.sv:224` | hit/miss/merge fail、first/secd、不同 mask和互异数据，逐 byte对比 reference line model | P1 |
| BIU 请求 | AR 属性/ID | CA/NC/SO/atomic/sync类型决定地址、size、burst、ID和发出条件；BIU输出区见 `srcs/xx_lsu_rb.sv:380` | 每种属性单独与资源反压，检查 request稳定到grant、ID唯一且禁止非法 US NC/SO进入总线 | P0 |
| LFB 协同 | create/hit ID | cacheable miss申请 LFB，RID按 LFB项编码并处理 hit-existing；LFB接口见 `srcs/xx_lsu_rb.sv:163`～`175` | LFB empty/full/hit same line，分配后延迟响应；检查 ID、owner和回收顺序一致 | P0 |
| R response | beat/错误 | entry按 ID接收 R beat，unit-stride要求恰好两拍并处理 response error；response逻辑见 `srcs/xx_lsu_rb_entry.sv:1250` | 正常2拍、缺拍、多拍、错 ID、每拍 error；协议负向激励必须触发 assertion | P0 |
| B response | paired owner | sync/fence/atomic 与 WMB共享的 B response只可归属已接受 paired write；B response逻辑见 `srcs/xx_lsu_rb_entry.sv:1540` | B在 request前/同拍/后到达，插入无关固定 ID响应；检查 owner匹配或明确报错 | P0 |
| SO 队列 | ID FIFO | SO多 outstanding通过 FIFO把 response映射到最老 entry；SO输入见 `srcs/xx_lsu_rb.sv:200` | FIFO empty/full/wrap，多 entry响应顺序与乱序负向场景；检查队首映射和无旧 owner | P1 |
| WB | completion/data grant | ready entry分别申请 completion/data并保持 payload到grant；WB接口由 `srcs/xx_lsu_rb.sv:380` 后部汇总 | 多 entry同 ready、WB反压0/1/N拍；断言 grant onehot、公平合同和 payload稳定 | P0 |
| Flush | sync/async 差异 | full/check flush处理年轻请求；debug async flush可不等待卡死 outstanding，但 resume需隔离旧响应；输入见 `srcs/xx_lsu_rb.sv:180`、`197` | request前后注入三类 flush；debug-only允许未响应进入debug，resume场景检查旧 RID不命中新 entry | P0 |
| Clock/Reset | entry门控 | create、response、state和WB grant每次状态更新都需对应 gateclk；entry状态位于 `srcs/xx_lsu_rb_entry.sv:190` | ICG on/off/scan和reset边界，功能事件孤立一拍；比较 enable、实际 clock pulse和寄存变化 | P1 |

RB-RR-04 类继承设计只有在本工程的 ID 编码、唯一 outstanding 和 paired owner合同等价时才可引用 C910 waiver；本计划保留负向 response-owner 检查。

<!-- RB-INTERACTION-2.1-BEGIN -->

## Interaction 2.1 叶级场景与执行合同

共72行；每个父功能点6行。状态只允许 `BLOCKED_NO_VCS` 或 `PENDING_FULL_CHIP`。
- Scoreboard键为 `{entry, IID, generation, BIU ID, owner}`，entry复用必须递增generation。
- unit-stride R response必须恰好两拍；B response必须按BIU ID与原owner配对。
- sync flush、check flush与async flush分别验证，不把不可取消的总线事务误报为新副作用。

|场景|setup/周期|当|则|checker/coverage/关闭|
|---|---|---|---|---|
|`RB-FP-01-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lda0_rb_ex3_create_vld=1` 且存在可用entry时；触发信号 `lda0_rb_ex3_create_vld`|则 `rb_lda0_ex3_full=0` 且create只占用一个entry；交付信号 `rb_lda0_ex3_full`|`CHK_RB_FP01_CAPACITY` / `COV_RB_FP01_CAPACITY`；scoreboard为每次分配记录entry、IID和generation，保留项不被普通create占用|
|`RB-FP-01-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_lda0_ex3_full` 保持二态且只归属于同一owner|`CHK_RB_FP01_CAPACITY` / `COV_RB_FP01_CAPACITY`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-01-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_lda0_ex3_full` 只反映文档定义的winner且不含X|`CHK_RB_FP01_CAPACITY` / `COV_RB_FP01_CAPACITY`；winner唯一，loser payload零次误消费|
|`RB-FP-01-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_lda0_ex3_full` 不得产生被flush owner的新副作用|`CHK_RB_FP01_CAPACITY` / `COV_RB_FP01_CAPACITY`；flush后的completion/create/forward计数均不增加|
|`RB-FP-01-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_lda0_ex3_full` 仅在所属accept窗口有效且 `$isunknown(rb_lda0_ex3_full)=0`|`CHK_RB_FP01_CAPACITY` / `COV_RB_FP01_CAPACITY`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-01-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_lda0_ex3_full` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP01_CAPACITY` / `COV_RB_FP01_CAPACITY`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-02-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当LD0、LS0和LS1 create同拍竞争时；触发信号 `lda0_rb_ex3_create_vld`|则仲裁只接受可用entry数量允许的owner且每个payload保持原IID；交付信号 `rb_lda0_ex3_full`|`CHK_RB_FP02_CREATE_ARB` / `COV_RB_FP02_CREATE`；三路payload使用互异IID和地址，winner与entry映射唯一|
|`RB-FP-02-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_lda0_ex3_full` 保持二态且只归属于同一owner|`CHK_RB_FP02_CREATE_ARB` / `COV_RB_FP02_CREATE`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-02-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_lda0_ex3_full` 只反映文档定义的winner且不含X|`CHK_RB_FP02_CREATE_ARB` / `COV_RB_FP02_CREATE`；winner唯一，loser payload零次误消费|
|`RB-FP-02-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_lda0_ex3_full` 不得产生被flush owner的新副作用|`CHK_RB_FP02_CREATE_ARB` / `COV_RB_FP02_CREATE`；flush后的completion/create/forward计数均不增加|
|`RB-FP-02-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_lda0_ex3_full` 仅在所属accept窗口有效且 `$isunknown(rb_lda0_ex3_full)=0`|`CHK_RB_FP02_CREATE_ARB` / `COV_RB_FP02_CREATE`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-02-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_lda0_ex3_full` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP02_CREATE_ARB` / `COV_RB_FP02_CREATE`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-03-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当create被接受后BIU grant和R response依次到达时；触发信号 `lda0_rb_ex3_create_vld`|则entry按create、request、response、WB顺序推进且每个终态一次；交付信号 `rb_biu_ar_req`|`CHK_RB_FP03_LIFECYCLE` / `COV_RB_FP03_LIFECYCLE`；entry复用前generation递增，迟到响应不得命中新owner|
|`RB-FP-03-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_biu_ar_req` 保持二态且只归属于同一owner|`CHK_RB_FP03_LIFECYCLE` / `COV_RB_FP03_LIFECYCLE`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-03-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_biu_ar_req` 只反映文档定义的winner且不含X|`CHK_RB_FP03_LIFECYCLE` / `COV_RB_FP03_LIFECYCLE`；winner唯一，loser payload零次误消费|
|`RB-FP-03-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_biu_ar_req` 不得产生被flush owner的新副作用|`CHK_RB_FP03_LIFECYCLE` / `COV_RB_FP03_LIFECYCLE`；flush后的completion/create/forward计数均不增加|
|`RB-FP-03-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_biu_ar_req` 仅在所属accept窗口有效且 `$isunknown(rb_biu_ar_req)=0`|`CHK_RB_FP03_LIFECYCLE` / `COV_RB_FP03_LIFECYCLE`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-03-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_biu_ar_req` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP03_LIFECYCLE` / `COV_RB_FP03_LIFECYCLE`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-04-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当 `lda0_rb_ex3_merge_vld=1` 且地址命中已有cache line时；触发信号 `lda0_rb_ex3_merge_vld`|则hit_idx有效；跨boundary或不兼容属性时merge_fail有效且不污染旧payload；交付信号 `rb_lda0_ex3_hit_idx`|`CHK_RB_FP04_MERGE` / `COV_RB_FP04_MERGE`；same-line、different-line、boundary-first和boundary-second全部交叉|
|`RB-FP-04-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_merge_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_lda0_ex3_hit_idx` 保持二态且只归属于同一owner|`CHK_RB_FP04_MERGE` / `COV_RB_FP04_MERGE`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-04-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_merge_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_lda0_ex3_hit_idx` 只反映文档定义的winner且不含X|`CHK_RB_FP04_MERGE` / `COV_RB_FP04_MERGE`；winner唯一，loser payload零次误消费|
|`RB-FP-04-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_merge_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_lda0_ex3_hit_idx` 不得产生被flush owner的新副作用|`CHK_RB_FP04_MERGE` / `COV_RB_FP04_MERGE`；flush后的completion/create/forward计数均不增加|
|`RB-FP-04-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_merge_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_lda0_ex3_hit_idx` 仅在所属accept窗口有效且 `$isunknown(rb_lda0_ex3_hit_idx)=0`|`CHK_RB_FP04_MERGE` / `COV_RB_FP04_MERGE`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-04-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_merge_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_lda0_ex3_hit_idx` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP04_MERGE` / `COV_RB_FP04_MERGE`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-05-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当可请求entry成为BIU owner且LFB非full时；触发信号 `lda0_rb_ex3_create_vld`|则 `rb_biu_ar_req=1`，AR地址、长度、属性和BIU ID都属于同一entry；交付信号 `rb_biu_ar_req`|`CHK_RB_FP05_BIU_AR` / `COV_RB_FP05_BIU_AR`；grant前请求稳定，grant后每个entry只发一次AR|
|`RB-FP-05-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_biu_ar_req` 保持二态且只归属于同一owner|`CHK_RB_FP05_BIU_AR` / `COV_RB_FP05_BIU_AR`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-05-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_biu_ar_req` 只反映文档定义的winner且不含X|`CHK_RB_FP05_BIU_AR` / `COV_RB_FP05_BIU_AR`；winner唯一，loser payload零次误消费|
|`RB-FP-05-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_biu_ar_req` 不得产生被flush owner的新副作用|`CHK_RB_FP05_BIU_AR` / `COV_RB_FP05_BIU_AR`；flush后的completion/create/forward计数均不增加|
|`RB-FP-05-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_biu_ar_req` 仅在所属accept窗口有效且 `$isunknown(rb_biu_ar_req)=0`|`CHK_RB_FP05_BIU_AR` / `COV_RB_FP05_BIU_AR`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-05-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_biu_ar_req` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP05_BIU_AR` / `COV_RB_FP05_BIU_AR`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-06-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当RB owner要求linefill且LFB可接受时；触发信号 `lda0_rb_ex3_create_vld`|则LFB create valid、地址和BIU ID属于同一RB owner；交付信号 `rb_lfb_create_vld`|`CHK_RB_FP06_LFB` / `COV_RB_FP06_LFB`；LFB full/hit/grant交叉不重复创建且依赖wakeup不串entry|
|`RB-FP-06-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_lfb_create_vld` 保持二态且只归属于同一owner|`CHK_RB_FP06_LFB` / `COV_RB_FP06_LFB`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-06-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_lfb_create_vld` 只反映文档定义的winner且不含X|`CHK_RB_FP06_LFB` / `COV_RB_FP06_LFB`；winner唯一，loser payload零次误消费|
|`RB-FP-06-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_lfb_create_vld` 不得产生被flush owner的新副作用|`CHK_RB_FP06_LFB` / `COV_RB_FP06_LFB`；flush后的completion/create/forward计数均不增加|
|`RB-FP-06-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_lfb_create_vld` 仅在所属accept窗口有效且 `$isunknown(rb_lfb_create_vld)=0`|`CHK_RB_FP06_LFB` / `COV_RB_FP06_LFB`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-06-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_lfb_create_vld` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP06_LFB` / `COV_RB_FP06_LFB`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-07-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当匹配BIU ID的unit-stride R response到达时；触发信号 `biu_lsu_r_vld`|则恰好两拍数据按同一entry组装；错误响应只标记原owner并抑制正常完成；交付信号 `rb_lwb_ex3_data_req`|`CHK_RB_FP07_R_RESPONSE` / `COV_RB_FP07_R`；0/1/N拍响应、错误beat和entry立即复用均以BIU ID及generation判定|
|`RB-FP-07-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `biu_lsu_r_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_lwb_ex3_data_req` 保持二态且只归属于同一owner|`CHK_RB_FP07_R_RESPONSE` / `COV_RB_FP07_R`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-07-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `biu_lsu_r_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_lwb_ex3_data_req` 只反映文档定义的winner且不含X|`CHK_RB_FP07_R_RESPONSE` / `COV_RB_FP07_R`；winner唯一，loser payload零次误消费|
|`RB-FP-07-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `biu_lsu_r_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_lwb_ex3_data_req` 不得产生被flush owner的新副作用|`CHK_RB_FP07_R_RESPONSE` / `COV_RB_FP07_R`；flush后的completion/create/forward计数均不增加|
|`RB-FP-07-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `biu_lsu_r_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_lwb_ex3_data_req` 仅在所属accept窗口有效且 `$isunknown(rb_lwb_ex3_data_req)=0`|`CHK_RB_FP07_R_RESPONSE` / `COV_RB_FP07_R`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-07-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `biu_lsu_r_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_lwb_ex3_data_req` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP07_R_RESPONSE` / `COV_RB_FP07_R`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-08-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当atomic或store相关entry收到匹配的B response时；触发信号 `biu_lsu_b_vld`|则completion、exception和IID只归属于原entry owner；交付信号 `rb_lwb_ex3_cmplt_req`|`CHK_RB_FP08_B_RESPONSE` / `COV_RB_FP08_B`；B response按BIU ID配对，迟到或重复B不得命中已复用entry|
|`RB-FP-08-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `biu_lsu_b_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_lwb_ex3_cmplt_req` 保持二态且只归属于同一owner|`CHK_RB_FP08_B_RESPONSE` / `COV_RB_FP08_B`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-08-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `biu_lsu_b_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_lwb_ex3_cmplt_req` 只反映文档定义的winner且不含X|`CHK_RB_FP08_B_RESPONSE` / `COV_RB_FP08_B`；winner唯一，loser payload零次误消费|
|`RB-FP-08-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `biu_lsu_b_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_lwb_ex3_cmplt_req` 不得产生被flush owner的新副作用|`CHK_RB_FP08_B_RESPONSE` / `COV_RB_FP08_B`；flush后的completion/create/forward计数均不增加|
|`RB-FP-08-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `biu_lsu_b_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_lwb_ex3_cmplt_req` 仅在所属accept窗口有效且 `$isunknown(rb_lwb_ex3_cmplt_req)=0`|`CHK_RB_FP08_B_RESPONSE` / `COV_RB_FP08_B`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-08-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `biu_lsu_b_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_lwb_ex3_cmplt_req` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP08_B_RESPONSE` / `COV_RB_FP08_B`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-09-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当strong-order entry创建并等待响应时；触发信号 `lda0_rb_ex3_create_vld`|则SO FIFO按请求顺序维护ID且pending地址属于队首owner；交付信号 `rb_wmb_so_pending`|`CHK_RB_FP09_SO_FIFO` / `COV_RB_FP09_SO`；FIFO empty/full、连续push/pop和response同拍不丢ID|
|`RB-FP-09-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_wmb_so_pending` 保持二态且只归属于同一owner|`CHK_RB_FP09_SO_FIFO` / `COV_RB_FP09_SO`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-09-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_wmb_so_pending` 只反映文档定义的winner且不含X|`CHK_RB_FP09_SO_FIFO` / `COV_RB_FP09_SO`；winner唯一，loser payload零次误消费|
|`RB-FP-09-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_wmb_so_pending` 不得产生被flush owner的新副作用|`CHK_RB_FP09_SO_FIFO` / `COV_RB_FP09_SO`；flush后的completion/create/forward计数均不增加|
|`RB-FP-09-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_wmb_so_pending` 仅在所属accept窗口有效且 `$isunknown(rb_wmb_so_pending)=0`|`CHK_RB_FP09_SO_FIFO` / `COV_RB_FP09_SO`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-09-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_wmb_so_pending` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP09_SO_FIFO` / `COV_RB_FP09_SO`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-10-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当响应完成的entry请求WB且grant到达时；触发信号 `lwb_rb_ex3_cmplt_grnt`|则completion/data请求保持到grant，winner数据和IID来自同一entry；交付信号 `rb_lwb_ex3_cmplt_req`|`CHK_RB_FP10_WB` / `COV_RB_FP10_WB`；completion与data独立反压，任一grant不会清除另一未完成请求|
|`RB-FP-10-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lwb_rb_ex3_cmplt_grnt=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_lwb_ex3_cmplt_req` 保持二态且只归属于同一owner|`CHK_RB_FP10_WB` / `COV_RB_FP10_WB`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-10-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lwb_rb_ex3_cmplt_grnt=1` 与同级竞争条件同拍成立时|则 C1 `rb_lwb_ex3_cmplt_req` 只反映文档定义的winner且不含X|`CHK_RB_FP10_WB` / `COV_RB_FP10_WB`；winner唯一，loser payload零次误消费|
|`RB-FP-10-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lwb_rb_ex3_cmplt_grnt=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_lwb_ex3_cmplt_req` 不得产生被flush owner的新副作用|`CHK_RB_FP10_WB` / `COV_RB_FP10_WB`；flush后的completion/create/forward计数均不增加|
|`RB-FP-10-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lwb_rb_ex3_cmplt_grnt=1` 且目标payload取边界值时|则 C1至C3 `rb_lwb_ex3_cmplt_req` 仅在所属accept窗口有效且 `$isunknown(rb_lwb_ex3_cmplt_req)=0`|`CHK_RB_FP10_WB` / `COV_RB_FP10_WB`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-10-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lwb_rb_ex3_cmplt_grnt=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_lwb_ex3_cmplt_req` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP10_WB` / `COV_RB_FP10_WB`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-11-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当live entry遇到sync flush或async flush时；触发信号 `lda0_rb_ex3_create_vld`|则可取消owner不再发出新的BIU、LFB或WB副作用；不可取消事务仍按协议收尾；交付信号 `rb_biu_ar_req`|`CHK_RB_FP11_FLUSH` / `COV_RB_FP11_FLUSH`；sync flush、async flush与check flush分别验证，迟到响应不得复活已清entry|
|`RB-FP-11-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_biu_ar_req` 保持二态且只归属于同一owner|`CHK_RB_FP11_FLUSH` / `COV_RB_FP11_FLUSH`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-11-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_biu_ar_req` 只反映文档定义的winner且不含X|`CHK_RB_FP11_FLUSH` / `COV_RB_FP11_FLUSH`；winner唯一，loser payload零次误消费|
|`RB-FP-11-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_biu_ar_req` 不得产生被flush owner的新副作用|`CHK_RB_FP11_FLUSH` / `COV_RB_FP11_FLUSH`；flush后的completion/create/forward计数均不增加|
|`RB-FP-11-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_biu_ar_req` 仅在所属accept窗口有效且 `$isunknown(rb_biu_ar_req)=0`|`CHK_RB_FP11_FLUSH` / `COV_RB_FP11_FLUSH`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-11-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_biu_ar_req` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP11_FLUSH` / `COV_RB_FP11_FLUSH`；scoreboard generation递增且旧响应零次命中新owner|
|`RB-FP-12-S01`|复位释放、无flush且所有非目标输入idle；C0: 驱动名义输入；C1: 采样目标输出|当ICG关闭、scan开启或reset施加在create边界时；触发信号 `lda0_rb_ex3_create_vld`|则scan允许状态捕获，reset后RB为空且所有请求为0；交付信号 `rb_empty`|`CHK_RB_FP12_CLOCK_RESET` / `COV_RB_FP12_CLOCK`；所有entry gate、pointer gate和special clock边界无X或幽灵owner|
|`RB-FP-12-S02`|先建立同一owner并施加两拍合法反压；C0: 建立owner；C1: 首次采样；C2: 保持输入再次采样|当 `lda0_rb_ex3_create_vld=1` 且同一owner连续保持两拍时|则 C1至C2 `rb_empty` 保持二态且只归属于同一owner|`CHK_RB_FP12_CLOCK_RESET` / `COV_RB_FP12_CLOCK`；两拍保持期间payload hash和owner均不漂移|
|`RB-FP-12-S03`|目标请求与次级来源同拍，二者payload使用互异花纹；C0: 同拍驱动竞争来源；C1: 采样winner；C2: 检查loser未被消费|当 `lda0_rb_ex3_create_vld=1` 与同级竞争条件同拍成立时|则 C1 `rb_empty` 只反映文档定义的winner且不含X|`CHK_RB_FP12_CLOCK_RESET` / `COV_RB_FP12_CLOCK`；winner唯一，loser payload零次误消费|
|`RB-FP-12-S04`|先建立live owner，再在转移边界施加full flush；C0: 建立owner；C1: full flush=1；C2: 撤销后采样|当 `lda0_rb_ex3_create_vld=1` 与 `rtu_yy_xx_flush=1` 在owner窗口交叉时|则 C2 `rb_biu_ar_req=0` 且 `rb_empty` 不得产生被flush owner的新副作用|`CHK_RB_FP12_CLOCK_RESET` / `COV_RB_FP12_CLOCK`；flush后的completion/create/forward计数均不增加|
|`RB-FP-12-S05`|目标字段取全零、全一和边界花纹，响应分别延迟0/1/N拍；C0: 驱动边界花纹；C1: 0拍采样；C2: 1拍采样；C3: N拍后采样|当 `lda0_rb_ex3_create_vld=1` 且目标payload取边界值时|则 C1至C3 `rb_empty` 仅在所属accept窗口有效且 `$isunknown(rb_empty)=0`|`CHK_RB_FP12_CLOCK_RESET` / `COV_RB_FP12_CLOCK`；0/1/N延迟与零/最大值交叉全部命中|
|`RB-FP-12-S06`|完成owner A后下一合法拍复用资源给互异owner B；C0: 驱动owner A；C1: accept；C2: 驱动owner B；C3: 采样|当 `lda0_rb_ex3_create_vld=1` 的资源在下一合法拍被新owner复用时|则 C3 `rb_empty` 只对应owner B，旧owner不得迟到修改|`CHK_RB_FP12_CLOCK_RESET` / `COV_RB_FP12_CLOCK`；scoreboard generation递增且旧响应零次命中新owner|

<!-- RB-INTERACTION-2.1-END -->
